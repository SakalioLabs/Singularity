# M4 Benchmark Evidence

This directory stores compact `m4-fixed-v1` preflight, runtime manifest, result, eligibility, and failure-ledger evidence. Offline fixtures are marked ineligible and never count toward repeat verification.

Each episode commits the canonical `session.json`; local JSONL and logger-summary duplicates are ignored.
