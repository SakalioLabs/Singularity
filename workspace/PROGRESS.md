# PROGRESS.md -- Evidence-Backed Progress

## Convergence Result

M1, M2, and M3 are `repeat_verified`. M1 has 15 distinct state-grounded benchmark successes. M2 has 23 eligible current-protocol successes across BM-006..010, including 3/3 accepted pairs for both composite tasks. M3 has three distinct raw-log-verified later-session retrieval/outcome pairs plus an approved held-out transfer gate. M4 is `failing`: BM-011 is repeat_verified at 3/3, BM-012 has twenty-three failed live attempts and remains 0/3, and BM-013..014 remain unverified. See `workspace/evals/capability_evidence_current.json` for the canonical state.

## Current Assessment

Singularity has broad source coverage and a large passing offline test surface, but the full Minecraft Agent system is not complete. The authoritative capability report is `workspace/evals/capability_evidence_current.json`.

Official Paper 1.20.4 build 499 is hash-pinned and all counted runs use hash-verified protocol identities. M1 remains complete at 15/15, M2 contributes 23 eligible current-protocol successes, and M4 now contributes three independently eligible BM-011 episodes with unique session, episode, level, and session hashes. The overall system remains incomplete.

## Stone Pickaxe Skill Convergence

The dedicated stone-pickaxe project is at Phase 148 under unchanged base protocol `stone-pickaxe-skill-fixed-v1`. SP-001 Acquire Cobblestone and SP-002 Craft Stone Pickaxe remain 3/3 with executable versions `learned:acquire_cobblestone@1.1.0` and `learned:craft_stone_pickaxe@1.0.1`. SP-003 has consumed thirty-five baseline authorizations and is now 1/1 baseline while candidate extraction remains 0/3: twenty-nine gameplay failures, five infrastructure failures, and one strict eligible baseline success. Phase 136 repair commit `9af7db44` is pushed and its retained event-96 replay advances from two verified plus one pending log source to three verified and zero pending in one observation, removes the fourth-log target, and permits the exact 12-plank craft.

## Iron Pickaxe SP-004 Continuation

SP-004 is `offline_ready_live_unverified`. Its fixed objective starts from the retained SP-003 terminal shape and requires exactly eight distinct stone removals, ten distinct coal-ore removals, three distinct iron-ore removals, an observed or newly made crafting table, one furnace craft and placement, one zero-retry three-item raw-iron smelt using coal, and one iron-pickaxe craft. It grants no capability or M4 credit.

The executable stack now includes a bounded `smelt` action across the isolated SP-004 Planner, ActionMapper, ActionVerifier, ActionController, Python Bridge, and `src/bot/sp004_bot_server.js`. The frozen shared Planner and `bot_server.js` remain byte-identical for historical evidence. The SP-004 backend accepts only supported output/input/fuel contracts, checks exact materials and an observed nearby furnace before mutation, refuses occupied furnace slots, makes one attempt with no automatic retry, and reports success only after inventory settlement proves output gain, input loss, and fuel loss.

`IronPickaxeSP004RuntimeAgent` adds a 24-block bounded scan, machine stage and target projection, exact action guard, monotonic result-backed progress, and terminal GoalVerifier. The strict Planner has an exact eight-node root graph and one-action stage instructions including `smelt`. The independent episode verifier reconstructs progress from retained action results rather than trusting supplied state. `scripts/iron_pickaxe_sp004_episode_runner.py` accepts the configured `grok-4.5` OpenAI-compatible endpoint, reads its key only from process environment, requires explicit single-run authorization, rejects contaminated initial inventory before actions, refuses evidence overwrite, and emits immutable preflight/session/episode/verification/manifest artifacts.

Focused validation passes: 23 SP-004 runtime/runner tests, 5 Node smelt cases, 4 Python smelt/action-stack cases, and all 98 SP-003 runtime regressions. No live SP-004 episode has run yet, so the requested stone-pickaxe-to-iron-pickaxe loop remains unproven in Minecraft.

Phase 138 replayed the Phase 137 root state without Minecraft: one zero-retry fixed-provider request returned the exact five-node graph and expected first move in `5.625s`; evidence commit `21bd9764` is pushed. Phase 139 authorization commit `45aa6a8d` was then consumed exactly once by episode `sp003_baseline_20260720_150508_7a645e08`. Preflight, reset, empty inventory, Paper, Bridge, and five log sources passed. Planner calls 0-2 were real, schema-valid, and zero-retry; three actions produced two machine-bound log removals, while one raw pickup failure was fully reconciled by the existing action-result policy. The Phase 136 observation-only branch was not needed. Continuation call 3 then received zero bytes and failed with `APIConnectionError -> ConnectError -> ConnectError -> SSLEOFError`, ending `empty_plan`. Manifest `1ead5fe3...` and derived failure `80abdfa5...` classify the run as `infrastructure_ineligible` with `intervention_not_exercised_new_blocker`.

Phase 140 tooling commit `859114b8` reconstructed a representative continuation request from the exact retained pre-call state: inventory `dark_oak_log:2`, two verified removals, no pending pickup, and target `dark_oak_log:118:141:-38`. The no-Minecraft probe made one request with zero retries, matched request SHA `a93b634c...`, received zero response bytes, and ended with `Connection error.` after `4.952s`. Immutable artifact `966c214f...` starts no Minecraft process, creates no authorization, and grants no baseline, skill, capability, or M4 credit. Focused Phase 140 checks pass 5/5, all stone-pickaxe checks pass 399/399, and the full repository passes 1152/1152; all 1822 deliverable JSON files parse and compilation, diff, and credential gates pass. Live authorization remains false until this evidence is pushed and a new bounded continuation-provider probe passes.

Phase 141 tooling commit `009a3cfe` made one new zero-retry request from the same immutable state. The provider recovered: it returned a real, schema-valid continuation in `4.108s` with exact `dig dark_oak_log @ 118,141,-38`. Raw artifact `2c67ac6a...` reports `passed=false` only because the probe expected `source_id` before the real SP-003 action guard ran. Exact retained-state replay through `stone-pickaxe-sp003-action-guard-v2` is allowed with no issues and deterministically adds `source_id=dark_oak_log:118:141:-38`, yielding the exact expected action. This is a probe-evaluator false negative, not a provider failure. Phase 141 checks pass 8/8, all stone-pickaxe checks pass 407/407, the full repository passes 1160/1160, and all 1823 deliverable JSON files parse; compilation, diff, and credential gates pass. The raw artifact remains unchanged; live authorization is false until it is pushed and the guard-normalized evaluator repair is independently implemented, verified, and pushed.

Phase 142 tooling commit `ac945428` now evaluates exactly one provider action only after production guard normalization. Immutable audit `d5f1f7c0...` binds the Phase 141 response and Phase 139 machine state, reclassifies the provider recovery as passed, and rejects unobserved coordinates, wrong log family, non-nearest same-family target, and an extra forged parameter. Phase 142 checks pass 7/7, all stone-pickaxe checks pass 414/414, the full repository passes 1167/1167, and all 1825 deliverable JSON files parse; compilation, diff, and credential gates pass. It performs no provider request, starts no Minecraft process, creates no authorization, and grants no baseline, skill, capability, or M4 credit. Live authorization remains false until this reclassification evidence is pushed, after which a separate parent-bound one-use baseline authorization may be created.

Phase 143 pushes Phase 142 evidence at `6bd489f2`, consumes exactly one separate authorization from `5b82e7bb`, and retains episode `sp003_baseline_20260720_173513_afba21cd`. All 23 Planner calls are real and schema-valid with one attempt and zero retries; ten actions succeed, including three distinct logs, exact planks and sticks, one crafted table, one same-level shaft egress, and two machine-proven surface clearances. After the second clearance, the raw first target correctly carries `navigation_only=true` and `stone_clearance_shaft_step_up_egress=true`, but the compact Planner target whitelist omits the step-up marker and the stage instruction recognizes only older clearance/approach booleans. Call 13 therefore describes all booleans as false and emits `place` instead of the required exact `move_to`; ten step-up candidates are rejected without backend invocation or world mutation until the 300-second deadline. Manifest `b8c901cc...` binds all thirteen retained files. Evidence commit `1ebbb35a` is pushed and synchronized. Evidence and predecessor checks pass 38/38, all stone-pickaxe checks pass 417/417, the full repository passes 1170/1170, and all 1837 deliverable JSON files parse; compilation, diff, and credential gates pass. No retry, baseline, skill, capability, or M4 credit occurs.

Phase 144 preserves both shaft-egress markers in the bounded Planner target contract and makes `navigation_only=true` authoritative during table staging. Targets with `stand_position` require its exact x/y/z; older navigation targets without it retain their proven position x/z path. The exact Phase 143 wrong `place` is now suppressed before dispatch for one fresh-observation replan per fingerprint, while the exact step-up `move_to` is guard-allowed and normalized with inventory preservation. Unsafe skill context still fails closed without replan. Offline audit `77f94ee9...` is schema-valid and binds the immutable Phase 143 session. Focused checks pass 7/7, Phase 134-144 evidence checks pass 45/45, all stone-pickaxe checks pass 424/424, the full repository passes 1177/1177, and all 1839 prospective deliverable JSON files parse; compilation, diff, and credential gates pass. No provider request, Minecraft process, authorization, baseline credit, skill credit, capability credit, or M4 credit occurs. The repair must be committed and pushed before one bounded no-Minecraft provider probe may run; live authorization remains false.

Phase 144 repair commit `c30ff138` and Phase 145 probe-tooling commit `154380e7` are pushed and synchronized. The Phase 145 no-Minecraft probe binds the exact Phase 143 call-13 step-up observation and repaired request SHA `8b66dcaf...`. Exactly one real provider call and one transport request returned a schema-valid continuation in `2.858s` with exact `move_to(120.5,141,-36.5)`; the production Phase 122 effective guard allowed it with no issues and normalized it to tolerance `1.0` plus `preserve_inventory=true`. Immutable LF-normalized evidence `3e116811...` passes every criterion and its Draft 2020-12 schema. Focused checks pass 6/6, Phase 134-145 evidence checks pass 51/51, all stone-pickaxe checks pass 430/430, the full repository passes 1183/1183, seven Node suites pass 84/84, and all 1841 controlled JSON files parse; compilation, diff, and credential gates pass. No retry, Minecraft process, authorization, baseline credit, skill credit, capability credit, or M4 credit occurs. Live authorization remains false until this evidence is committed and pushed, after which one separate parent-bound one-use baseline authorization may be created.

Phase 145 evidence commit `30634355`, Phase 146 one-use authorization commit `d9d27969`, and Phase 146 failure-evidence commit `3242b4b0` are pushed and synchronized. Episode `sp003_baseline_20260720_194725_phase146` consumed that authorization exactly once. Preflight, reset, empty inventory, Paper, Bridge, and five visible log sources passed, but root Planner call 0 made one transport attempt with zero retries and failed before receiving any response bytes with `APIConnectionError -> ConnectError -> ConnectError -> SSLEOFError`. The run ended `empty_plan` after zero executable actions, so neither the empty-hand behavioral loop nor the Phase 144 step-up intervention was exercised. Manifest `f2bb7f56...` and derived classification `8fe9fc1b...` retain the run as `infrastructure_ineligible` with `intervention_not_exercised_new_blocker`; they grant no baseline, skill, capability, or M4 credit. Focused checks pass 4/4, the selected Phase 134-146 evidence chain passes 55/55, all stone-pickaxe checks pass 432/432, the full repository passes 1187/1187, seven Node suites pass 84/84, and all 1855 controlled JSON files parse; compilation, diff, credential, port, and owned-process cleanup gates pass.

Phase 147 tooling commit `d64af344` is pushed and synchronized. Its single no-Minecraft probe bound the immutable Phase 146 root observation and TLS EOF call, then made exactly one provider chat call and one transport request with zero retries. A real schema-valid response returned in `5.375s` with the exact five-node root graph and exact first `move_to(121,142,-36)` action. Immutable evidence `c78d6153...` passes all 20 criteria and its Draft 2020-12 schema. Focused checks pass 5/5, the selected Phase 134-147 evidence chain passes 60/60, all stone-pickaxe checks pass 437/437, the full repository passes 1192/1192, seven Node suites pass 84/84, and all 1857 controlled JSON files parse; compilation, diff, and credential gates pass. Evidence commit `bcf35e6c` and the separate Phase 148 authorization commit `3056719f` are pushed. The probe itself starts no Minecraft process and grants no success, skill, capability, or M4 credit.

Phase 148 consumes authorization `3056719f` exactly once in episode `sp003_baseline_20260720_211838_phase148`. Fresh-world reset proves an empty inventory and no table or pickaxe. All 23 Planner calls are real and schema-valid, with one transport attempt each and zero retries; all 23 actions succeed. The machine-verifiable chain removes three distinct oak logs, performs the exact plank, stick, table, wooden-pickaxe, and stone-pickaxe crafts, places one table, equips the wooden pickaxe, removes exactly three distinct stone sources, and ends with stable `stone_pickaxe:1`. The five-node graph, SP-001 component, SP-002 component, and all 51 strict verifier criteria pass with zero raw or unreconciled action failures in `146.235s`. Manifest `55c9026a...` binds all twelve payloads; thirteen retained run artifacts are byte-hash verified. This is the first eligible SP-003 baseline success and proves the full empty-hand behavioral loop, but candidate extraction remains 0/3 and capability/M4 counts remain unchanged. No further live run is authorized until this evidence is committed and pushed and a separate parent-bound candidate `r1` authorization is created.

The fixed protocol pins Minecraft/Paper/Mineflayer, seed and fixture contracts, Planner/provider/model, Action Backend, GoalVerifier, TaskSystem, bounded skill mode, task-specific episode deadlines and cycle limits, initial inventories, crafting-table observation policy, exact `stone` source allowlist, machine success, and evidence eligibility. SP-003 binds that protocol byte-for-byte and adds a five-node empty-hand graph, exact mutation counts, one-attempt crafts, a fresh-world reset-only substrate, stable re-observation, and separate baseline/candidate attribution rules.

Supplemental policy `stone-pickaxe-sp002-controlled-runtime-v1` still binds the unchanged protocol, executable acquire skill artifacts, exact fixture, one craft action, one backend attempt, and delayed stable reobservation. Attempts 3, 4, and 5 independently validate the exact `-3 cobblestone/-2 stick/+1 stone_pickaxe` path and complete the three-success extraction gate. SP-003 now has a separate fail-closed policy, runtime module, runner, launcher, and dedicated tests; this engineering readiness is not live capability evidence. Automatic retry, additional SP-002 evidence authorization, capability, and M4 authority remain false.

Supplemental policy `stone-pickaxe-sp001-paired-evaluation-v1` binds the exact advisory record, candidate queue record, promotion artifact, fixture tree, and three retained baseline evidence bundles without modifying the base protocol. The baseline index is 3/3 with equal fixed-control and initial-state fingerprints. The paired report remains deliberately `retain_advisory`: candidate pairs are 0/3, all three support arms are verified, and consumed candidate `r1` failed despite matching fixed controls and initial state. Fifteen dedicated offline cases prove exact authorization, one-arm consumption, advisory runtime exclusion, source/metric recomputation, duplicate rejection, and the separate `1.1.0` review boundary.

SP-002 recovery policy `stone-pickaxe-sp002-paired-evaluation-v2` is an offline-only, non-overlapping window. It binds v1 policy `f050c964...`, v1 report `54a836ec...`, and failed `shadow-1` run `a5efc29f...`; excludes all v1 support/candidate IDs; and permits only fresh `shadow-2`, `advisory-2`, `fallback-2`, and candidate `r4..r6` under one-use parent-bound authorizations. The isolated agent invokes support modes before the first root Planner call and, for candidates, preserves the required LLM root plan and subtasks before overlaying the bounded skill action. Fresh `shadow-2` and `advisory-2` are live passes; fallback remains unrun and candidate pairs remain 0/3, so the report is still `retain_advisory`. No capability credit, M4 credit, normal-runtime permission, or SP-003 permission is created by this evaluation.

Offline fixtures and failed SP runs are explicitly non-capability evidence. The canonical capability report is unchanged; all eight pre-existing baseline skill records remain hash-protected, and the acquire 1.0.0 advisory record remains canonical-hash identical after append-only promotion. M1/M2/M3 remain `repeat_verified`, BM-012 remains 0/3 after twenty-three failed attempts, and M4 remains `failing`. Authorization commit `be9e770f` produced exactly one r12 runtime-start attempt. Paper opened port 25565, then its JVM failed a 1,103,256-byte native allocation while system free virtual memory was only 708MB; Bridge never reached bot readiness. The unrelated Nuxt process tree consuming roughly 165GB of committed memory was stopped exactly, restoring about 100GB of free virtual memory. Authorization, restoration, four credential-clean runtime logs, and a schema-valid derived failure record are retained; the JVM crash report itself is excluded because it carries process-environment and host metadata. V5 policy identity and 13 dedicated cases pass. R13, r14, and r15 evidence are pushed at `24f28911`, `fe15719a`, and `69f3c5f0`. The separate offline promotion reconstructed the v5 report exactly from retained runs, created executable version 1.1.0 with rollback target 1.0.0, and produced an approved fingerprint-bound runtime gate. No live process ran. Automatic retry, capability, M4, SP-002 live, and SP-003 authority remain false.

BM-012 remains 0/3 after twenty-three attempts. Probe 23 ran exactly once from frozen committed and pushed gate commit `f528ea17`. Its authorization was consumed by `autonomous_start` event 1 at monotonic 132071.343; no retry or Probe 24 ran. The Agent started 10 goals, completed six, failed three, and interrupted one across 82 cycles. It executed 47/73 actions successfully and made 82 Planner calls: 80 real, 78 schema-valid real responses, two schema-invalid responses, two transport failures, and 289117 total tokens.

The failed-dependency machine-state reconciliation intervention was not exercised. Observation event 563 proved exact `wooden_pickaxe:1` and action event 573 recorded `+1`. Existing reconciliation event 572 completed seven nonterminal equivalent pickaxe tasks but reported zero failed-dependency reconciliations. Failed tasks `006ddc9b`, `7c9d9eee`, and `c395cbb4` remained terminal; none was a direct dependency of an active-frontier task, so no live requirement fingerprint, reconciliation event, latency, dependent/root readiness transition, or history-preservation transition exists.

The scheduler emitted zero `task_readiness_recovery_goal` events, directly reselected no terminal failed task after machine satisfaction, and created no recovery child. The active frontier contained seven accepted equivalent pickaxe tasks before the machine boundary and zero after event 572. Event 583 next selected `Mine 12 cobblestone for stone tools and furnace`; twelve successful stone digs raised cobblestone to 12 and terminal inventory retained 8.

The new earliest failure layer is `m4_ready_task_failed_root_machine_state_disconnect`. Task `f3622be3` (`Craft torches`) was selected as a ready task at event 934 after dependency `7346f403` completed. It failed on task deadline at event 956. Event 980 then machine-crafted `torch:+4`, but generic goal verification could not complete the already-failed bound root.

Events 984-1558 produced 30 `suppress_until_bound_task_machine_completion` decisions across 23 cycles until dusk interrupted the root at event 1562. This was the failed selected root itself, not a failed direct dependency, so the Probe 23 policy correctly did not qualify. No stone-pickaxe plan or craft, iron goal, approach, dig, pickup, or positive iron delta followed.

Probe 23 is classified exactly as `intervention_not_exercised`, not infrastructure-ineligible. Preflight, reset, fixed controls, protocol identity, runtime health, and authorization checks passed. The Agent crossed the absolute deadline by 0.016 seconds. No action occurred after the deadline, but one timeout error plan returned after it, so independent eligibility passed 66/74 checks and rejected the episode.

The bounded `m4-failed-dependency-machine-state-reconciliation-v1` repair passes offline. A failed or blocked direct dependency is reconciled only from a structured, machine-verifiable inventory postcondition on the active frontier. Exact item matching is the default; family aggregation requires an explicit trusted internal contract. The reconciliation preserves the previous terminal status, original failure reason/result/transition, attempts, blockers, and observations while adding proof, observation identity, requirement fingerprint, state generation, timestamp, and a deterministic event ID.

The scheduler no longer returns a terminal failed dependency title directly. An unmet requirement can create only one active bounded recovery child per fingerprint, with a new task ID, parent/root provenance, and a three-attempt budget. Existing root-completion binding is reused after successful reconciliation, and full reconciliation runs after every M4 post-action machine observation. Probe 23 grants no capability upgrade: next authorization is false, BM-012 remains 0/3, M4 remains `failing`, and BM-013/BM-014 remain locked.

Validation passes across seven gate cases, 106 Memory/TaskSystem definitions, GoalVerifier, M1/M2/M3 regressions, six fixed Node suites with 52 internal assertions, Python compilation, all repository JSON artifacts, capability consistency, credential scan, and repository checks. M1, M2, and M3 remain `repeat_verified`.

Current report outcome:

- Readiness: `rejected`
- Claim audit: `approved`
- System status: `incomplete`
- Declared-complete phases audited: 4 (M0, M1, M2, M3)
- Supported completion claims: 4 (M0, M1, M2, M3)
- Contradicted completion claims: 0 after status correction
- Unsupported completion claims: 0 after status correction
- Partial live phases: 0
- Failing live phases: 3 (M4, M5, M6)
- Repeat-verified runtime phases: 3 (M1, M2, M3)

## Canonical Capability State

| Phase | Canonical status |
|---|---|
| M0 | `source_verified` |
| M1 | `repeat_verified` |
| M2 | `repeat_verified` |
| M3 | `repeat_verified` |
| M4 | `failing` |
| M5 | `failing` |
| M6 | `failing` |
| M7 | `not_run` |

## M2 Convergence Progress

- Phase 2 protocol construction is complete: `m2-fixed-v1` covers BM-006..010 with the pinned compatible endpoint and `deepseek-v4-flash`, structured root/continuation/replan calls, dependency-bearing subtasks, independent world-state verification, and strict evidence eligibility.
- Phase 3 live convergence is complete: BM-006 and BM-007 each have three eligible baseline/candidate pairs, BM-008..010 each have three independent eligible successes, and the canonical recovery gate accepts two sessions.
- A real Paper BM-010 empty-execution smoke passed reset/observation checks and was correctly rejected by the terminal verifier. A second real Paper smoke executed the bounded template, placed 55 blocks, and passed the exact 5x5 shelter verifier.
- Those smoke reports are deliberately non-capability evidence. They establish harness readiness but contain no LLM Root Plan and do not count toward M2.
- A real API preflight returned valid JSON plus complete token usage, and two controlled BM-006 baselines reached the live planner. The first exposed three noncanonical dig parameter shapes; the typed dig contract then removed that failure in the next run.
- The latest run advanced from 0 actions accepted beyond navigation to two successful digs, four dark-oak planks, one valid Root Plan, and two complete task paths. It still produced no crafting table and remains ineligible.
- M2 action feedback now captures the pre-action task before a consumptive post-state can invalidate its input precondition; the exact one-log-to-four-planks lifecycle regression passes offline. A live rerun hit a transient Root Plan connection error before exercising the path.
- A subsequent live rerun produced a valid Root Plan and two successful grounded digs, but no craft. The continuation prompt exposed `required_action_types` without exposing the already successful dig, so the planner redundantly dug a second log before a malformed third response ended the run.
- Added `m2-successful-action-summary-v1`: at most eight successful current-goal actions, typed targets/positions/inventory deltas, and a 1600-character hard bound are injected separately from memory and copied into planner-call evidence.
- The next live run proved the intervention: after one dig, continuation selected plank craft; after that craft, continuation selected crafting table and explicitly recognized that required dig/craft action types were already satisfied. No redundant dig occurred, and `craft_planks` completed.
- The run lasted 317.71 seconds despite BM-006's fixed 240-second limit. In-flight planner requests crossed the deadline, and actions were still executed at elapsed 245.14 and 319.92 seconds.
- Added `m2-hard-total-deadline-v1`: planner requests receive only remaining goal time minus a 30-second action window, use zero provider retries, and are rejected if they return after that window. Agent checks suppress late plans/actions, and both the session validator and canonical log adapter reject an exceeded duration, a deadline event, or an action timestamp after the fixed limit.
- The first post-fix live run proved the request metadata path with decreasing 209.921/185.750/164.078-second budgets, zero retries, a 93.797-second goal duration, and no post-deadline action. It exposed a new earlier action-contract gap when the model emitted `craft.recipe` instead of the verifier-required `craft.item`.
- Added an exact `craft.item` schema with optional positive integer `count`, rejecting `recipe` before execution, and added auditable `goal_elapsed_s/max_duration_s` result fields. Four unchanged live reruns then encountered provider `Connection error` on the Root Plan before any action; a separate non-counting full-prompt probe succeeded in 32.875 seconds.
- A cooldown rerun validated the new craft contract in Minecraft: one log became four dark-oak planks, the `craft_planks` task completed, and continuation selected a crafting table. The next action was rejected because the old verifier treated recipe `oak_planks:4` as an exact item requirement.
- Added `m2-machine-verifier-v2` with pinned `minecraft-planks-tag-v1` ingredient semantics. Any supported Minecraft 1.20.4 plank combination can satisfy a plank recipe ingredient, while terminal item criteria remain exact.
- Default thinking mode later exhausted all 4096 output tokens with no visible content. M2 now fixes `thinking=disabled`, requires a stopped visible response with zero reasoning bytes, and uses `m2-bounded-transport-retry-v1`: zero SDK retries plus at most one typed `APIConnectionError` retry after client reset and deadline recomputation.
- A 39.328-second live run then produced the requested crafting table but exposed an exact-`oak_log` machine-verifier defect. BM-006 now pins ten accepted log/stem source blocks while preserving block-removal and pickup requirements; the same prior trace passes only as non-counting replay evidence.
- The next controlled baseline was the first eligible M2 success under its protocol revision: 26.23 seconds benchmark duration, 19.891 seconds goal duration, three successful actions, three complete task paths, `crafting_table:+1`, GoalVerifier achieved, and zero deadline issues in session `4bb9d423-4ec`.
- Its matching candidate also completed BM-006 but selected no skill. The retained trace reported `inventory:oak_planks>=4` twice despite four dark-oak planks, so the pairing gate correctly marked the comparable pair ineligible.
- `m2-bounded-skills-v2` extends the already pinned `minecraft-planks-tag-v1` policy to learned-skill preconditions and contract readiness. Offline tests accept dark-oak and mixed plank stacks at a total of four, while rejecting three planks and non-plank inventory.
- Three fresh BM-006 pairs then passed under protocol SHA-256 `deeff43e51b03ba435db03c7f8760b0279d8d45190dd5bca69cd0c24c75100bb`. All six arms passed; all three candidates selected and successfully executed `learned:craft_crafting_table@1.0.1`; no arm recorded an action failure.
- The canonical BM-006 gate is 3/3 eligible pairs and `repeat_verified`.
- BM-007 converged at 3/3 eligible pairs. Stable crafting confirmation now rejects Mineflayer inventory ghosts, waits through a bounded cooldown before retrying, and records each attempt. Local skill success is attributed from verified skill postconditions rather than unrelated suffixes of the broader goal. Historical failures, quarantine, attribution corrections, and the approved-gate restoration of 1.0.2 remain auditable.
- BM-008 completed three independent `equip → move_to → dig` coal runs. One malformed root response remains retained as a failed run; a later accepted run also provides recovery evidence.
- BM-009 completed three independent two-craft runs after task guidance made the no-table torch recipe and fixed initial resource budget explicit. The prior 36-action table/place detour remains retained failure evidence.
- BM-010 completed three independent bounded builds. Each placed 55 episode-delta blocks, preserved the two-block entrance and clear interior, covered 25/25 roof positions, moved the player inside, and emitted complete dependent task-state paths. Earlier schema and state-path failures remain retained.
- The canonical M2 state is now `repeat_verified` with no missing evidence and an approved composite pairing/recovery gate. M4-M7 were not started.

## Engineering Delivered

- Mineflayer bridge, structured observations, canonical action control, safety verification, and session logging.
- Rule and LLM planning, hierarchical tasks, readiness reporting, deterministic prerequisite recovery, reflection, and goal verification.
- Layered memory, task continuity, typed retrieval, promptware filtering, attribution gates, skill memory, skill lifecycle, and transfer gates.
- Autonomous curriculum, runtime interrupts, world-model/frontier reports, causal evidence, and controlled self-evolution reports.
- Vision analysis, optional screenshot plumbing, visual action grounding, and screenshot evidence validation.
- Multi-agent shared state, role execution, bridge preflight, schedule comparison, and single-agent baseline machinery.
- Evidence and ablation tooling for action verification/value, memory policy, plan cache, mixed initiative, coaching, visual review, and runtime profiles.
- Machine-checkable M3/M5/M6 live adapters with distinct-session deduplication and transfer/world-model/visual-action support gates.
- Portable canonical evidence inventory with content hashes, session identities, protocol hashes, evidence kinds, and counting eligibility for every consumed report and source log.
- Added `m2-fixed-v1`, strict `m2-root-plan-v1` validation, real-call metadata and token accounting, root-task lifecycle evidence, failure-triggered replanning, independent terminal observations, and canonical M2 session/pair deduplication.
- Added the bounded `build_shelter_5x5` Mineflayer action and verifier: exact construction zone, allowlisted material, episode block delta, wall/roof/entrance/interior geometry, inventory consumption, and protected-area occupancy are checked from live observations.
- Rule and LLM planning cycles now retrieve typed memory through strict per-read and per-decision character budgets, emit a schema-v2 contract, and exclude non-planning causal reads from bounded-context evidence. Rule selection remains world-state-driven and reports that memory has not yet influenced its action choice.
- Autonomous runs now emit observations, plans, selected goals, and terminal subgoal outcomes as non-nested machine-checkable events; queued tasks no longer silently replace the goal whose plan is being executed.
- Task continuity now uses schema-v2 execution-state records with session, branch, parent/root checkpoint, depth, validation, and revision provenance. Planner context follows one active root-to-current path and labels failed/proposed branches as hints; revision commands remain review-only and cannot restore world state.
- Added a MemGym-style fixed-control lineage ablation plus a verifier-backed shadow-restoration report and gate. Built-in lineage fixtures reduce failed-branch contamination from 2 to 0 with 2/3 review-ready cases; they remain synthetic and cannot approve the gate.
- Player observations and action snapshots now preserve dimension, hunger, saturation, oxygen, XP, game mode, and selected slot so future shadow comparisons can reject hidden state rollback rather than comparing inventory and position alone.
- Replaced planner-facing rich task-continuity injection with a budgeted Goal Frontier Capsule while retaining the full durable ledger. Built-in cases preserve all identity/frontier/continuation probes, keep failed checkpoint IDs out of the capsule, and reduce fair flat-baseline context by 103, 74, and 86 characters respectively (87.7 average). Bounded-context replay now cross-checks the declared budget, observed characters, and sanitized trace while allowing a valid empty capsule before the first checkpoint; these remain offline fixture results, not a live token-cost claim.
- Added `frontier_transition_skill_router_v1`, which reranks governed skills against readiness tasks, assigned skills, missing preconditions, target state, task family, and Bayesian-smoothed use outcomes. Its 600-character planner projection logs only skill names, task IDs, scores, coverage, and reason codes. Fixed-control built-ins improve expected top-1 routing from 0/3 to 3/3 with full frontier coverage and zero regressions; this is synthetic offline evidence only.
- Added a verifier-calibrated soft-retirement pipeline. It measures false-pass bias per judge with injected defects, compares each learned skill to a fixed-control no-skill baseline, requires live provenance and distinct sessions, and applies approved results only as a task-family runtime overlay. Built-ins cannot be quarantined, synthetic fixtures are runtime-ineligible, skill files are never mutated, and every gate hard-codes `automatic_delete_allowed=false`.
- Upgraded AgenticCache reuse with Progressive Crystallization and EvoSOP lifecycle constraints. Schema-v2 cache reports can grant only hybrid planner guidance from offline traces; runtime reports aggregate exact executable-workflow matches, independent sessions, goal outcomes, action failures, and verifier rejects per entry. Three matched successes across three complete live sessions are required by default for deterministic reuse, while regressions demote only the affected entry.
- Added a hosted-API-compatible recall-controlled episode viability cascade. `behavior_surface_v1` scores only typed action/verifier/progress events, calibrates exact binomial survival bounds per round, searches budgets on a separate validation split, certifies global success recall on held-out test data, and binds `shadow`/`active` runtime use to planner, action backend, verifier, task stream, and seed identities. The implementation explicitly makes no hidden-activation or token-savings claim.
- Added `frontier_information_budget_v1` with an exact uniform control, fixed integer planner-round ledger, deterministic soft allocation, exploration floor, typed prerequisite/verifier/novelty/risk signals, and BAGEN-style remaining-round intervals. Active episode-abort savings can become bounded recovery credit but never extend the ledger. `frontier-rollout-budget-report` accepts offline fixtures for shadow review yet permits planner-facing advice only from distinct paired uniform/information JSONL sessions whose allocations replay exactly, outcomes do not regress, interval coverage has an exact one-sided certificate, and any recovered rounds link to an approved abort gate. Automatic retry, branch execution, and budget extension are structurally impossible.
- Added AgentRx/AgentTether-style critical-transition diagnosis. `minecraft_transition_unit_v1` normalizes both action cycles and actionless planner responses; deterministic constraints feed a temporal/plan/target/artifact/error dependency graph and `first_unrecovered_constraint_v1`. Five synthetic failures match 5/5 critical units and categories versus 3/5 for a recency baseline. Repair candidates are typed and review-only, with no planner, retry, intervention, memory-promotion, or skill-mutation authority.
- Fixed Mineflayer navigation truthfulness: `move_to` now uses pathfinder and succeeds only inside target tolerance, absent/null Y values preserve horizontal goals, action/socket timeout budgets align without replay, and unreached partial navigation defers the remaining plan suffix before any dependent world action.
- Added `m1-fixed-v1` as the shared Node/Python source of truth for BM-001..005, including canonical fixtures, BM-004's five-cobblestone threshold, cycle/time limits, runtime identities, and exact Mineflayer dependency versions.
- Added a reset-capable controlled runner that creates one fresh level per task, verifies all reset postconditions, forces the minimal RuleBasedPlanner profile, records the exact Paper jar hash, and refuses multi-task M1 acceptance runs in one episode.
- Pinned and provisioned official Paper 1.20.4 build 499, verified its published SHA-256, generated EULA/config files without accepting the EULA, and prepared the localhost-only fixed server configuration.
- Added state-grounded M1 transition validation. Dig must remove an observed source block and increase the target inventory; craft must increase the requested item; copied sessions, repeated episodes/hashes, nested reset payloads, and mixed server jars are ineligible.
- Added a bounded post-dig pickup window and closer grounded resource approach so the post-action observation can prove pickup instead of racing the item entity.
- Completed M1 live convergence: exact oak targeting, per-species 32-block tree visibility, measured navigation recovery, grounded drop collection, deterministic stone search, Minecraft-data drop mapping, target-specific pickup waits, and non-replayed dig commands now support 15/15 eligible successes.

## Evidence That Still Matters

- M1 has 15 eligible live successes: BM-001..005 each 3/3. Every counted session has verified reset, action transition, terminal criteria, and an achieved Goal Verifier event.
- The retained runtime blocker and eight convergence failures remain ineligible historical evidence; they are not rewritten or counted as successes.
- The capability report independently deduplicates session IDs, episode IDs, logs, hashes, and Paper jars and now reports M1 as `repeat_verified`.
- The new tracked critical-transition replay localizes all five historical M1 failures: four contain 100 actionless non-terminal plans each, while the only 200-action run first exhibits repeated no-progress navigation. These diagnoses have no manual critical-unit labels and do not upgrade M1 evidence.
- M2 has 23 eligible current-protocol successes: BM-006 has six across three accepted pairs, BM-007 has eight including three accepted pairs, and BM-008..010 each have three. The retained BM-010 harness/template smokes remain explicitly excluded from capability counting.
- No three-run first-night survival evidence is available.
- M3 now has three eligible later-session skill retrieval/outcome cases at attribution confidence 1.0 and an approved held-out gather transfer gate; all source logs pass content-hash and fixed-protocol checks.
- Existing M5 traces cover 22 moving sessions and pass the world-model feedback gate, but complete 0 of 27 goals.
- Existing M6 traces contain no verified screenshots and no live-source visual-action interventions.
- No live BM-701 multi-agent execution report is available.
- The older 37-session M3 report remains retained negative history; it no longer overrides the newer bounded-loop evidence accepted by the canonical report. M5 and M6 remain failing.
- The bounded-memory and autonomous-event fixes apply only to future sessions. They do not upgrade or rewrite the historical evidence above.
- Execution-state lineage, capsule probes, and shadow-state invariants are offline-verified but have no live Minecraft ablation or restoration evidence. The gate now requires positive context reduction in eligible cases and can only authorize shadow revision selection after repeated evidence; it always emits `automatic_restore_allowed=false`.
- Frontier skill routing has no fresh Minecraft completion, interaction-step, token, or latency comparison. The old ranker remains available through `--no-skill-frontier-routing` for live fixed-control runs.
- Skill soft retirement has deterministic offline coverage but no live defect-injection calibration or paired no-skill Minecraft traces. No current skill is justified for runtime quarantine by tracked evidence.
- Workflow crystallization is offline-verified, but no existing Minecraft cache entry has the three-session live evidence required for deterministic execution. Existing schema-v1 gates are intentionally rejected.
- Episode early-abort mechanics are offline-verified only. Existing logs do not provide disjoint successful calibration/validation/test populations, so no live gate is approved and runtime remains off by default.
- Frontier-budget mechanics are offline-verified only. The three built-in prerequisite cases conserve an eight-round budget and target later synthetic resolutions better than uniform, but they provide only three interval observations and no connected paired Minecraft sessions. No advisory runtime gate is approved; default mode remains off and shadow mode cannot change planner context.

## Frozen Research Backlog

The material below is retained as historical context and is not active work while the M1 convergence freeze is in force.

- MineExplorer: hidden prerequisite graphs and rule-based milestones should define progress rather than final-text claims.
- MineNPC-Task: task attempts should be scored against explicit dependencies, bounded knowledge, and machine-checkable validators.
- AgenticSTS: every planner decision should use bounded typed retrieval instead of accumulating raw transcripts.
- WorldLines: memory should preserve visibility, state revisions, and action-native evidence under partial observability.
- SelfMem: memory-policy variants should be optimized offline from retrieval cost and downstream outcomes, then promoted only through existing gates.
- MAGE execution-state memory: active task paths, completed checkpoints, failed branches, and revision boundaries should be represented separately before automatic rollback is enabled.
- MemGym: execution-state memory must be evaluated with the planner and action backend held fixed so memory gains are not confounded with reasoning or tool improvements.
- Goal-Oriented Graphs: planner-facing memory should preserve the active goal-to-prerequisite frontier rather than return disconnected relevant checkpoints.
- OpenClaw and Hermes compression: keep the complete durable record outside the prompt, budget only the injected projection, and verify recall/artifact/continuation/decision retention with probes before promotion.
- OpenClaw and Hermes: durable memory, procedural skills, maintenance passes, and workspace separation are useful only when action authority and promotion gates remain explicit.
- SkillReranker: match skills to decomposed execution-state intervals and unresolved prerequisites rather than the root goal alone.
- Gold-standard lightweight game agents: hold a strong deterministic yardstick fixed and report negative results and regressions instead of grading an agent against itself.
- Blind Curator and SkillCenter: scale skill libraries only with provenance, verifier-visible failures, and false-pass-calibrated soft retirement; never delete skills from an LLM judge score alone.
- Progressive Crystallization and EvoSOP: treat exploration as workflow discovery, keep offline procedures hybrid, promote entries individually after repeated live verification, and demote regressions without erasing evidence.
- Recall-Controlled Early Abort: use exact sample-complexity-aware certificates and shadow deployment before behavior-changing termination; behavior-only Minecraft signals must not inherit hidden-state probe claims.
- IGRPO: when retries or branch rollouts become available, allocate a fixed planner-round budget toward frontier nodes that reduce verifier uncertainty or reveal prerequisites, and compare against uniform allocation before online suppression.
- BAGEN: treat planner rounds as an active progressive ledger, report conservative remaining-cost intervals, and measure optimistic misses separately from task completion.
- AgentTether: localize retry value along prerequisite/dependency paths, carry fixed versus unresolved state across attempts, and use cooldown/minimal-intervention guards rather than blind retry.
- AgentRx and TrajAudit: audit guarded execution constraints, identify the first unrecovered transition, and keep full traces outside a compact dependency-linked evidence packet until manual labels establish localization quality.

## Immediate Sequence

1. Preserve and audit the immutable M1/M3 evidence sets and wooden-pickaxe quarantine history.
2. Keep independently promoted wooden-pickaxe `1.0.2` executable and historical `1.0.1` quarantined.
3. Keep the M2 protocol, paired evidence, retained failures, and recovery sessions immutable unless a tracked change requires full revalidation.
4. Leave M4-M7 unopened in this workstream; M4 is the next acceptance gate when a new convergence cycle is explicitly started.

## Stone-Pickaxe Convergence Runtime

- Sealed-fixture commit `aa8b7e6` remains synchronized in history; SP-001 is 3/3, SP-002 is 0/3, and SP-003 is locked. `learned:acquire_cobblestone@1.0.0` remains as immutable advisory history and `1.1.0` is executable with an approved runtime gate; the craft skill does not exist.
- Added a fail-closed survival-fixture and SP-001 runtime without changing M1/M2/M3 criteria, GoalVerifier thresholds, capability status, custom skills, BM-012, or prior probe evidence.
- Immutable snapshot identity covers canonical overworld/nether/end trees and is rechecked after restoration before Paper starts. Active benchmark reset and target-result injection are absent.
- The live path keeps skills off, gives action choice to the normal LLM Planner, bounds the whole goal to one absolute deadline and eight attempted actions, and independently verifies nearest-source selection, exact wooden-pickaxe equip, block removal, pickup, and terminal inventory delta.
- Offline validation passes 34/34 protocol cases, 32/32 existing runtime cases, and 15/15 dedicated SP-002 harness cases. The repository-wide non-live gate covers M1/M2/M3, GoalVerifier, TaskSystem, Skill runtime, ActionController, M4 deadlines, fixed Node suites, and Python compilation.
- Fixture session `sp_fixture_prep_20260715_143222_b0e58483` exposed the request-policy blocker: call 0 reached the completion limit with 15685 reasoning bytes and zero response bytes, so the Agent returned `empty_plan` after one cycle and zero actions. The repair sends exact `extra_body={"thinking":{"type":"disabled"}}`, uses one deadline-bounded zero-retry request, rejects empty output, suppresses post-deadline calls, and audits those controls before sealing or eligibility.
- Fixture session `sp_fixture_prep_20260715_152529_b99f05dd` proved the fixed request controls on its first two calls and executed two successful moves. The generic stone plan contract then admitted nine actions: the first dig suffix omitted `block`, three later craft suffixes used `recipe`, and ten tasks were created under two root IDs. The action guard rejected that dig before execution; call 2 later ended at `finish_reason=length` with truncated JSON, and the run stopped without retry.
- The second repair adds `stone-pickaxe-plan-v1`: a single root plan, two to six root subtasks, zero continuation/replan subtasks, exactly one immediate action, canonical action parameters, a 320-character reasoning bound, a 24-block compact machine state, same-root task identity, and failure-reason propagation. Offline replay now rejects the retained live plan for its action count, missing exact block, recipe aliases, and duplicate non-root subtasks before any action can execute.
- Fixture session `sp_fixture_prep_20260717_001130_678ddb1e` returned a 1869-character compact root with six subtasks and one action. The request finished `stop` with zero reasoning bytes, zero retries, and 2210 total tokens; the only schema issue was `subtask[5]:priority_invalid`, so no tasks or actions were created.
- The third repair makes the priority contract explicit: every root priority is a JSON integer in `1..5`, with `1` used when no ordering distinction is needed. Existing fail-closed validation is unchanged, and offline tests reject both `0` and string `"1"`.
- Fixture session `sp_fixture_prep_20260717_154044_81052541` executed six valid survival actions and reached `crafting_table:1`, six planks, and four sticks. The table remained in inventory, but the Planner emitted wooden-pickaxe craft without an observed nearby table. Seven backend attempts each failed with `crafting_table_found=false`; the root task graph also admitted unsupported `{"placed":true}`, and the downstream planner timeout exhausted the 300-second deadline.
- The fourth repair requires an observed table within 4.5 blocks before wooden-pickaxe craft, directs the Planner to place an inventory-only table first, rejects unsupported root task-state keys, and reconciles only dependency-ready machine-state tasks after every stone-pickaxe action observation. Offline replay rejects the retained impossible craft before backend execution and rejects the retained `placed` criterion.
- Fixture session `sp_fixture_prep_20260717_221520_064dd337` passed machine audit in 18 actions and 157.313 seconds. Its terminal state has exact `wooden_pickaxe:1`, zero target resources, and 13 reachable observed stone blocks. The sealed 45-file snapshot has tree SHA-256 `996b2a1f989626e9c44ddca5c24f81ae55a5dca03b246f0d72723c46fd6a7636`; independent audit rechecks its file count, byte count, components, protocol, and manifest identity.
- The first independent audit command exposed host compatibility with `Path.GetRelativePath`; the runtime now uses a repository-bounded legacy-compatible helper for both audit and SP-001. Post-action reconciliation no longer requires consumed crafting inputs to remain present before closing a machine-satisfied task. The real fixture audit rerun passes.
- SP-001 episode `sp001_episode_20260717_223525_23696e33` used pushed commit `aa8b7e6`, restored the exact fixture tree, passed request/deadline/eligibility controls, and stopped at `max_actions` after 35.188 seconds. All eight actions were successful `equip wooden_pickaxe`; the first changed main hand from `dark_oak_log` to `wooden_pickaxe`, but the compact planner state dropped equipment, so seven more equips consumed the budget. Distinct removals, pickups, and cobblestone delta were all zero.
- The repair exposes exact machine `held_item` from equipment slot 0 to the stone planner, requires advancement to the nearest stone when the wooden pickaxe is already held, and rejects redundant equip in the runtime guard. Retained-session replay and all ten SP-001 artifact hashes are tested. Action choice remains LLM-owned, automatic retry remains disabled, and the protocol JSON/SHA-256 are unchanged.
- SP-001 episode `sp001_episode_20260717_230318_23d8bdf3` used pushed commit `adc4b48` and ran five actions in 59.078 seconds. It eliminated redundant equips, removed four distinct stones, and ended with `cobblestone:4`. The second dig removed its block and spawned entity `322`, but `GoalNear(1)` resolved at measured distance `1.503`; the strict one-block fallback candidate set was empty, so that transition failed. The same entity survived and a later fallback collected it with another drop, producing delta 2, which proves delayed recovery but cannot retroactively satisfy per-transition provenance.
- The pickup repair keeps direct range 1, one fallback attempt, and inventory-grounded completion unchanged. It permits one safe adjacent standable candidate within a fixed 0.5-block selection margin; a candidate outside range still fails unless a real inventory delta occurs. The exact retained geometry is reproduced in Node and Python tests, and legacy dig behavior remains unchanged.
- SP-001 episode `sp001_episode_20260717_232454_5c05abf0` used pushed commit `4960717`, restored the same fixture tree, and produced the first eligible success. Four planner-owned actions (`equip`, then three `dig`) removed three distinct observed stones, proved three corresponding pickups and inventory deltas, ended with exact `cobblestone:3`, and passed all request, deadline, reset, intervention, machine-verification, and eligibility checks in 19.297 seconds.
- SP-001 episode `sp001_episode_20260718_012012_0349d399` used pushed commit `7bc7c2c` and independently reproduced the same eligible behavior in 21.422 seconds: four actions, zero failures, three distinct source removals, three grounded pickups, terminal `cobblestone:3`, no selected skill, and no forbidden intervention or post-deadline action.
- SP-001 episode `sp001_episode_20260718_013304_7c162864` used pushed commit `49a9fe1` but its sole root planning transport ended `APIConnectionError -> ConnectError -> SSLEOFError` before any response bytes. It stopped at `empty_plan` after 5.812 seconds with zero tasks, zero actions, identical initial/terminal inventory, no retry, and no world mutation. The machine auditor's generic pickup-fix suggestion is inapplicable to this zero-action trace; retained replay proves the existing fail-closed behavior and no runtime or protocol change is made.
- SP-001 episode `sp001_episode_20260718_014459_7a1a9b49` used pushed commit `73e3002` and retained the 2/3 gate after 8 actions and 178.437 seconds. It removed three stones but grounded only one pickup. On action 3, drop entity `321` remained at measured distance `1.503`; the safe fallback cell `(95,131,-32)` was within the fixed candidate margin at expected distance `1.425`, but it was also the player's current block cell at center distance `0.079`, so `GoalBlock` resolved without movement. The same entity was selected by the next dig, proving persistence; three later nearest-source guard rejects were downstream consequences.
- The third behavioral repair detects that exact current-cell alias and performs at most one 100ms forward micro-step toward the already validated safe candidate position. Forward control is always cleared, the fallback attempt limit remains one, and neither the nudge nor `GoalBlock` can report success without measured distance or a real inventory delta. Retained replay plus positive and negative Node cases prove both recovery and fail-closed behavior; protocol identity is unchanged.
- SP-001 episode `sp001_episode_20260718_022202_615c7b84` used pushed commit `9870a38`, but the sole root Planner request again ended `APIConnectionError -> ConnectError -> ConnectError -> SSLEOFError`. It stopped at `empty_plan` after 5.656 seconds with zero response bytes, tasks, transitions, actions, retries, or world mutation and exact initial/terminal inventory equality. The same-cell repair was never exercised; an independent retained replay confirms fail-closed behavior, so runtime and protocol remain unchanged.
- SP-001 episode `sp001_episode_20260718_023754_912de280` used pushed commit `cd2a6d2` after five unauthenticated TLS probes and one authenticated minimal completion passed. It independently completed in 23.391 seconds with four successful Planner-owned actions, three distinct source removals, three grounded pickups, terminal `cobblestone:+3`, and no action failure, selected skill, forbidden intervention, retry, or post-deadline action. Its episode, session, level, and session hash identities are distinct from the first two successes, completing SP-001 at 3/3.
- All four failed preparations, the successful non-counting preparation, all five failed SP-001 episodes, and all three successful SP-001 episodes remain immutable. Paper and Bridge stopped, ports `25565` and `30000` are clear, the fixture tree still audits to `996b2a1f989626e9c44ddca5c24f81ae55a5dca03b246f0d72723c46fd6a7636`, and all eighty SP-001 artifacts are hash-bound. No further SP-001 live episode is authorized.
- Candidate extraction preview exposed a generic `session_log_path` NameError and fixed-contract overreach. Pushed fix `64243de` repaired both, candidate commit `a40425f` froze `e1534e57`, and the separate promotion command then created `learned:acquire_cobblestone@1.0.0` as advisory. Queue, learning-ledger, promotion history, and skill record agree; the skill has no executable promotion gate and capability/M4 credit remain false. The eight pre-existing skill records now have individual canonical hash protection, allowing this authorized append without weakening history immutability. Paired evaluation remains locked pending an offline harness commit.
- Added the fail-closed SP-001 paired harness and supplemental policy. Three existing skill-off successes are hash-bound as baseline `r1/r2/r3`; their fixed controls and contract-relevant initial state match exactly. The live launcher can consume only one allowlisted arm/replicate from clean synchronized `main`, and the report refuses duplicates, source/metric drift, missing support evidence, current-version in-place promotion, or any normal-runtime/capability/M4 claim.
- Pushed harness commit `7f98556`, then ran exactly `shadow/shadow-1` once. Episode `sp001_skill_shadow_20260718_043048_bee49f0b` and session `f938ff45-28a` machine-verified SP-001 in 22.047 seconds with four planner calls, four successful actions, zero failed actions/verifier rejects/no-progress loops, three `skill_shadow_plan` events, and zero direct skill selections or executions. Its fixed-control and initial-state fingerprints exactly match all three baselines; all eleven retained run artifacts are hash-bound, runtime ports are clear, and the advisory skill record is unchanged.
- Pushed shadow evidence commit `58b96aa`, then ran exactly `advisory/advisory-1` once. Episode `sp001_skill_advisory_20260718_044057_bbcc998a` and session `0905b04c-439` machine-verified SP-001 in 18.531 seconds with four planner calls, four successful actions, zero failures/rejects/loops, three `skill_advisory_hint` events, and zero skill selections or executions. Controls/state again match every baseline; eleven new artifacts are hash-bound and runtime stopped cleanly.
- Pushed advisory evidence commit `4fc99ea`, then ran exactly `fallback/fallback-1` once. Episode `sp001_skill_fallback_20260718_044849_0893a248` and session `840b1c42-d86` machine-verified SP-001 in 18.812 seconds with four planner calls/actions, zero failures/rejects/loops, exactly one `skill_fallback`, and zero skill selections or executions. Ordinary planning recovered as required, controls/state match every baseline, and all eleven artifacts are hash-bound.
- Pushed fallback evidence commit `a80772f`, then ran exactly `candidate/r1` once. Episode `sp001_skill_candidate_20260718_051533_40094230` and session `f87a593f-d66` matched baseline controls/state but failed evaluation: the skill chose `stone:96:132:-32` while the SP-001 observation contract chose tied-nearest `stone:95:131:-32`, so the guard rejected the first skill dig without issuing a world command. Ordinary planning recovered three cobblestone in four later successful actions, but the retained run has one failed action, one skill fallback, zero skill completion, and no gate/capability/M4 credit. All eleven artifacts are hash-bound; `r1` is permanently consumed and automatic retry remains disabled.
- Pushed r1 evidence commit `b423059`, then reproduced the failure offline. The bounded skill runtime now honors the observation contract's distance and stable identity ordering, and an acquire phase emits exactly one dig so every mutation is followed by a fresh planning observation. The exact r1 tie replay is accepted by the unchanged SP-001 guard. Stone protocol is still 34/34, runtime is 31/31, paired evaluation is 15/15, all 40 non-live Python scripts and six Node suites pass, and no protocol, policy, retained run, skill record, or capability artifact changed.
- Pushed the runtime repair at `473b05a`, then defined isolated recovery window `stone-pickaxe-sp001-paired-evaluation-v2`. It leaves the v1 policy, harness, report, support runs, and failed `r1` byte-identical; permits only fresh candidates `r4/r5/r6`; imports shadow/advisory/fallback through exact v1 path, file-hash, policy-hash, and record-payload bindings; and maps the three original baselines one-to-one under new pair IDs. The initial v2 report is intentionally `retain_advisory` at 0/3 with inherited support 3/3 and explicit `r1` exclusion. Ten recovery-window cases and all fifteen v1 cases pass. No live v2 arm is authorized until this window commit is pushed.
- Recovery-window commit `fb472984` is pushed with exact local/remote tree identity. The next authorization is limited to one `candidate/r4` arm after its own ledger commit is pushed; `r5/r6`, support reruns, automatic retry, normal runtime, capability credit, and M4 credit remain disabled.
- Authorization commit `e8d0808` was pushed, then exactly one `candidate/r4` run executed. Episode `sp001_skill_candidate_20260718_063149_7f573331` and session `f84e2f7b-9ed` machine-verified the goal in 11.656 seconds with one real Planner call, four successful actions, zero failures or verifier rejects, exact three-cobblestone terminal progress, one skill selection, and no fallback. Evaluation still failed because `StonePickaxeRuntimeAgent._verify_action_for_execution` normalized each skill action with an in-place `clear/update`, deleting its nonsemantic `skill_context` before action logging and `_record_skill_usage`; all three planned skill coordinates nevertheless match the three successful dig actions. The eleven artifacts and updated v2 report are retained, `r4` is permanently consumed, v2 cannot reach 3/3, `r5/r6` are unauthorized, and the next work is offline-only provenance preservation followed by a fresh non-overlapping window.
- Pushed r4 evidence commit `31b9cc3`, then reproduced its shared-object mutation offline. The SP-001 guard now retains only a structured internal `skill_context` carrying `skill_id` while still replacing `type/parameters` with its normalized action, so nearest-source `source_id` enforcement is unchanged and successful actions reach `_record_skill_usage`. The exact regression keeps the plan context intact and records one successful attributed skill action. Protocol remains 34/34, runtime is 32/32, v1/v2 evaluation is 15/15 and 10/10, all 41 non-live Python scripts and six Node suites with 55 internal cases pass, and no live episode or evidence mutation occurred. A new non-overlapping recovery window is required before any further candidate run.
- Pushed provenance fix `36f03ee7`, then defined isolated recovery window `stone-pickaxe-sp001-paired-evaluation-v3` under its own byte-preserving evidence directory. It binds the exact v1/v2 policy and report hashes, immutable failed candidates `r1/r4`, runtime attribution code, and the unchanged three support runs. Candidate IDs `r7/r8/r9` and pair IDs are new; `r1..r6`, support reruns, retries, and normal runtime are rejected. The initial report is `retain_advisory` at 0/3 with inherited support 3/3 and no evidence mutation. V3 passes 11/11 cases alongside v1 15/15, v2 10/10, all 42 non-live Python scripts, six Node suites with 55 internal cases, compilation, and 1210 JSON artifacts. No live episode is authorized before this window commit is pushed and a separate one-arm `r7` authorization is committed and pushed.
- Recovery-window commit `2fc4e04b` is pushed with exact local/remote tree identity. This separate ledger commit authorizes only one `candidate/r7` episode after it is pushed; `r8/r9`, all superseded replicate IDs, support reruns, automatic retry, normal runtime, capability credit, and M4 credit remain disabled.
- Authorization commit `f263bad9` was pushed, then exactly one `candidate/r7` run executed. Episode `sp001_skill_candidate_20260718_074811_b3f3e698` and session `26574c9a-280` machine-verified SP-001 in 12.453 seconds with one Planner call, four successful actions, three verified and reobserved attributed skill digs, zero failures/rejects/fallbacks, and attribution confidence 1.0. The sole failed evaluation check was `skill_completed`: finalization discarded the selected subtask's `mining` route provenance, inferred `gathering` from the successful root goal, and recorded a false `routing_error` despite satisfied postconditions. All eleven artifacts, the updated v3 report `b5a4300...`, and skill-regression ledger `3b777e72...` are retained; `r7` is consumed, `r8/r9` are unauthorized, and no live permission remains.
- Pushed r7 evidence commit `2089ff89`, then reproduced the finalization defect offline. Skill selection now freezes its routed goal, fingerprint, and task family; finalization recomputes those values, rejects mismatches, and records the outer root family separately. The exact r7 replay attributes three successful mining actions despite the broader gathering root, while wrong-family and tampered routes remain fail-closed and non-attributable to skill quality. All 42 non-live Python scripts, six Node suites, compilation, and diff checks pass; no evaluation artifact, skill record, capability report, or live world state changed. A fresh non-overlapping window is the next gate.
- Pushed route-scope repair commit `18525025`, then defined isolated recovery window `stone-pickaxe-sp001-paired-evaluation-v4` under its own byte-preserving evidence directory. It binds the exact route fix, immutable v1-v3 reports and policies, all three failed run records, and unchanged support evidence. Only `r10/r11/r12` can enter the new window; `r1..r9`, support reruns, retries, and normal runtime are rejected. The initial report is `retain_advisory` at 0/3 with inherited support 3/3. Policy identity, 11/11 v4 cases, all 43 non-live Python scripts, six Node suites with 55 cases, compilation, 1274 JSON files, capability consistency, immutable hashes, credential scan, and diff checks pass. No live episode is authorized until this window commit is pushed and a separate one-arm `r10` authorization is committed.
- Recovery-window commit `8eaa6800` is pushed with exact local/remote tree identity. This separate ledger commit authorizes only one `candidate/r10` episode after it is pushed; `r11/r12`, all superseded replicate IDs, support reruns, automatic retry, normal runtime, capability credit, and M4 credit remain disabled.
- Authorization commit `5ad854d1` was pushed, then exactly one `candidate/r10` run executed. Episode `sp001_skill_candidate_20260718_085317_8e8de2cf` and session `ffdefabd-e30` machine-verified and evaluation-passed in 13.718 seconds with one Planner call, four successful actions, three verified and reobserved skill digs, one successful skill completion, zero failures/rejects/fallbacks, and attribution confidence 1.0. All eleven artifacts are hash-bound, v4 passes 12/12 evidence-aware cases, the report is `retain_advisory` at 1/3 with hash `ccb6acf6...`, Paper/Bridge stopped, and no live permission remains.
- R10 evidence commit `90e4e230` is pushed with exact local/remote tree identity. A recurrent non-network preflight check then exposed that nonzero local command probes could return `fail` or `warn` without a remedy. Every nonpassing command check now carries a deterministic shell-verification remedy, and the new forced-exit regression passes alongside 93/93 preflight cases, all 43 non-live Python scripts, six Node suites with 55 cases, compilation, 1284 JSON files, capability consistency, 224 immutable ledger hashes, credential scan, and diff checks. No live episode ran and r11 remains unauthorized pending this fix commit.
- Preflight fix `1f5a4061` is pushed with exact local/remote tree identity. This separate ledger commit authorizes exactly one `candidate/r11` episode after push; `r12`, all consumed or excluded IDs, support reruns, automatic retry, normal runtime, capability credit, and M4 credit remain disabled.
- Authorization commit `41ce0d14` was pushed, then exactly one `candidate/r11` run executed. Episode `sp001_skill_candidate_20260718_093639_8f6f185f` and session `b0c64ce3-d50` machine-verified and evaluation-passed in 15.750 seconds with one Planner call, four successful actions, three verified and reobserved skill digs, one successful skill completion, zero failures/rejects/fallbacks, and attribution confidence 1.0. All eleven artifacts are hash-bound, v4 passes 12/12 evidence-aware cases, the report is `retain_advisory` at 2/3 with hash `cad8a9ed...`, Paper/Bridge stopped, and no live permission remains.
- R11 evidence commit `ef40eec7` is pushed with exact local/remote tree identity. This separate ledger commit authorizes exactly one `candidate/r12` episode after push; all consumed or excluded IDs, support reruns, automatic retry, normal runtime, capability credit, M4 credit, SP-002, and SP-003 remain disabled.
- Authorization commit `be9e770f` was pushed, then r12 was attempted exactly once. Paper reached its TCP listener but failed during world startup with JVM native-memory exhaustion before Bridge bot readiness. No session, episode, evaluation run, Planner call, action, or skill observation was created. The authorization is permanently consumed, r12 is infrastructure-ineligible, the v4 report remains byte-identical at 2/3, and no retry is allowed. The exact unrelated Nuxt process tree responsible for host commit exhaustion was stopped; free virtual memory recovered from 708MB to about 100GB. A strict schema, derived failure record, authorization/restoration hashes, and four credential-clean runtime logs retain the evidence while excluding the environment-bearing JVM crash report. The next gate is an offline-only, non-overlapping v5 window.
- Defined isolated recovery window `stone-pickaxe-sp001-paired-evaluation-v5` without running Minecraft or changing runtime code. It permits only `r13/r14/r15`, excludes `r1..r12`, binds the immutable v4 policy/report and r12 infrastructure artifact plus schema/authorization/restoration hashes, and inherits the three v1 support arms exactly. Its initial report is 0/3 and `retain_advisory`; 12/12 dedicated cases reject prior-run reuse, duplicate authorization, missing window identity, and any retained-evidence tampering. The v5 commit must be pushed before a separate r13 authorization can exist.
- Pushed v5 definition commit `1e734f41` with exact local/remote tree identity. This separate ledger commit authorizes exactly one `candidate/r13` episode after push; `r14/r15`, all prior replicate IDs, support reruns, automatic retry, normal runtime, capability credit, M4 credit, SP-002, and SP-003 remain disabled.
- Authorization commit `02269113` was pushed, then exactly one `candidate/r13` episode ran. Episode `sp001_skill_candidate_20260718_112102_6a99724e` and session `bec8094e-147` machine-verified SP-001 in 12.438 seconds with one Planner call, four successful actions, zero failures/rejects/loops, one skill selection, three verified executions, one completion, and no fallback. Its controls and initial state match baseline r13, all eleven artifacts are hash-bound, the v5 report is 1/3 and `retain_advisory`, and r14 remains unauthorized until this evidence commit is pushed.
- R13 evidence commit `24f28911` is pushed with exact local/remote tree identity. This separate ledger commit authorizes exactly one `candidate/r14` episode after push; r15, all consumed IDs, support reruns, automatic retry, normal runtime, capability credit, M4 credit, SP-002, and SP-003 remain disabled.
- Authorization commit `2ebbdace` was pushed, then exactly one `candidate/r14` episode ran. Episode `sp001_skill_candidate_20260718_120904_1785ae0b` and session `e0669d82-9a9` machine-verified SP-001 in 16.032 seconds with one Planner call, four successful actions, zero failures/rejects/loops, one skill selection, three verified executions, one completion, and no fallback. Its controls and initial state match baseline r14, all eleven artifacts are hash-bound, the v5 report is 2/3 and `retain_advisory`, and r15 remains unauthorized until this evidence commit is pushed.
- R14 evidence commit `fe15719a` is pushed with exact local/remote tree identity. This separate ledger commit authorizes exactly one `candidate/r15` episode after push; all consumed IDs, support reruns, automatic retry, normal runtime, capability credit, M4 credit, SP-002, and SP-003 remain disabled.
- Authorization commit `fb6e434a` was pushed, then exactly one `candidate/r15` episode ran. Episode `sp001_skill_candidate_20260718_123511_a340128d` and session `18a71a99-e9f` machine-verified SP-001 in 11.859 seconds with one Planner call, four successful actions, zero failures/rejects/loops, one skill selection, three verified executions, one completion, and no fallback. Its controls and initial state match baseline r15, all eleven artifacts are hash-bound, and the v5 report is 3/3 with decision `review_executable_new_version`, approved readiness, and no normal-runtime permission pending a separate promotion review.
- R15 evidence commit `69f3c5f0` is pushed with exact local/remote tree identity. The separate offline promotion rebuilt the v5 report byte-equivalently from all retained run records, validated the exact gate, preserved `learned:acquire_cobblestone@1.0.0`, and appended executable `1.1.0` with parent and rollback version 1.0.0. Promotion artifact `d03a4e97...` and runtime gate `6a918872...` bind gate fingerprint `132028ca...`; four focused cases pass, a directory-local binary boundary preserves exact committed bytes, and a second real invocation changed no artifact hash. No Minecraft process or live authorization was used. SP-002 is unlocked only for its offline harness and a later separately committed authorization.
- Added the SP-002 offline harness and supplemental policy without changing the base protocol or retained evidence. Fixture preparation and live episodes have separate one-time parent-bound authorization scopes; both require clean synchronized `main`. The fixture builder starts from the immutable SP-001 snapshot and permits only normal survival actions, while the episode runner keeps skills off, admits one exact stone-pickaxe craft, forces the Bridge to one attempt, proves exact input consumption and table interaction, and captures a delayed stable observation. The next gate is a separately committed and pushed fixture-preparation authorization; no runtime started in this phase.
- Pushed harness commit `22c81939`, then pushed one fixture-preparation authorization at `6664082c` and consumed it exactly once. Episode `sp002_fixture_20260718_150018_18719678` and session `6a0b5945-25e` restored the immutable 45-file source fixture, passed authorization and Bridge protocol audits, and started Paper/Bridge cleanly. Its sole Planner call returned `Connection error.` with zero response bytes, so the run stopped after 5.265 seconds with zero tasks/actions, equal initial and terminal inventory, and no SP-002 fixture. Eleven credential-clean evidence files are hash-bound; the authorization is permanently consumed, no automatic retry occurred, and a fresh parent-bound authorization cannot be issued until this failure evidence commit is pushed.
- Archived the first consumed authorization at `75165b5a`, then pushed second authorization `a53abaaa` after a same-model transport preflight passed in 2.078 seconds. Episode `sp002_fixture_20260718_153300_63dd8d56` and session `2b281df0-f57` made five successful single-attempt Planner transports. Four schema-valid plans produced four verified successful actions: equip the wooden pickaxe and dig three distinct stone blocks. Terminal inventory had exact `cobblestone:3`, exact `stick:2`, and no stone pickaxe. Call 5 returned `finish_reason=stop` with zero reasoning bytes but declared `status=complete` while carrying one action; the immutable schema rejected `terminal_actions_forbidden` before execution because no interactive crafting table was observed. Eleven credential-clean artifacts and a byte-exact consumed-authorization archive retain the failure. No retry, fixture seal, live SP-002 episode, capability credit, or M4 credit occurred; the next gate is an offline-only prompt-contract regression.
- Reproduced and fixed the second fixture failure offline without changing the fixed protocol, SP-002 policy, frozen SP-001 runtime, or launcher. The compact prompt now derives `completion_ready` from exact three cobblestone, exact two sticks, absent stone pickaxe, and an observed table within 4.5 blocks. `completion_ready=false` forbids `complete`; every action-bearing response must be `planning`, and an action's predicted outcome cannot count before reobservation. The retained `complete` plus `move_to` shape still fails only with `terminal_actions_forbidden`, while a satisfied observation exposes `completion_ready=true` and permits only `complete` with `actions=[]`. SP-002 is 14/14, existing runtime is 32/32, protocol is 34/34, and M1/M2/Memory/GoalVerifier/M4 deadline regressions pass. No live process or authorization was created during the fix.
- Pushed the terminal-action repair as `f371ceba`, then pushed third authorization `759fd5a2` for episode `sp002_fixture_20260718_161807_484083ea`. The immutable source fixture re-audited at 45 files and 14,684,703 bytes, the same-model transport probe passed in 2.608 seconds with zero retries and zero reasoning bytes, and the host had more than 67GB physical and 98GB virtual memory free. The authorization was consumed exactly once. Session `0211ea2d-80c` made one successful real Planner transport, but the root response contained one action and zero subtasks; the unchanged validator rejected `root_subtask_count_out_of_bounds` and `root_dependency_edge_missing` before execution. Initial and terminal inventory are identical, all action counts are zero, Paper/Bridge stopped cleanly, and no fixture or snapshot was sealed. Eleven credential-clean local evidence artifacts, including the byte-exact authorization archive, retain the failure under the existing evidence policy. No retry, replacement authorization, capability credit, or M4 credit occurred.
- Reproduced and fixed the third fixture failure offline without changing the protocol, SP-002 policy, frozen SP-001 runtime, launcher, or root validator. An unsatisfied `prepare_sp002_fixture` root prompt now supplies the exact `acquire_cobblestone` then `observe_crafting_table` graph with one dependency edge and still permits only one immediate grounded action. Continuation calls explicitly require `subtasks=[]`. The retained one-action, zero-subtask root shape continues to fail only with `root_dependency_edge_missing` and `root_subtask_count_out_of_bounds`, while the exact graph passes. SP-002 is 15/15, all 46 non-live Python scripts and six fixed Node suites pass, 1,287 tracked JSON files parse, and 321 ledger path/hash bindings verify. No live process or authorization was created during the repair.
- Pushed the root-graph repair as `80fe5981`, then pushed fourth fixture-preparation authorization `c1a12d8e` for episode `sp002_fixture_20260718_170101_39bce6f7`. After one transient preflight TLS EOF, a separate zero-retry same-model probe passed in 2.093 seconds; the authorization was then consumed exactly once. Session `b80a2014-5bd` completed in 26.328 seconds with six schema-valid real Planner calls, six accepted actions, zero backend failures, and both exact graph nodes completed. The machine audit proved exact three cobblestone, exact two sticks, no stone pickaxe, survival/safe/movable state, and an interactive crafting table 1.414 blocks away. The independently re-audited 48-file, 14,962,077-byte snapshot tree is `e5201d33...`; Paper/Bridge stopped, controlled ports cleared, server properties restored, and the consumed authorization is archived. This fixture preparation does not count toward SP-002's 0/3 live gate, capability, or M4; the next action requires a separately committed and pushed `live_episode` authorization.
- Pushed the verified fixture evidence as `c287d083`, then pushed live authorization `3bb78347` for episode `sp002_live_20260718_173020_6a24cd3a`. The same-model transport probe passed in 2.718 seconds and the authorization was consumed exactly once. Session `9d2b5d45-b67` made one successful real Planner call with `finish_reason=stop`, zero reasoning bytes, and zero retries. The returned root plan had the correct one craft action but zero subtasks; the fixed validator rejected only `root_dependency_edge_missing` and `root_subtask_count_out_of_bounds` before execution. Initial and terminal inventories are identical at three cobblestone, two sticks, and no stone pickaxe; action and backend craft-attempt counts are zero. Seventeen credential-clean evidence artifacts retain the failure, Paper/Bridge stopped, ports cleared, and no retry or replacement authorization occurred.
- Replayed and fixed the first live SP-002 root-plan failure offline. The mode-specific root prompt now requires exactly `verify_inputs` followed by `craft_stone_pickaxe`, including the dependency edge, while still allowing only `craft stone_pickaxe count=1`; continuation calls still require `subtasks=[]`, and the validator is unchanged. The retained zero-subtask response continues to fail with exactly the original two issues, while the valid graph passes. SP-002 is 16/16, the existing runtime and protocol suites remain 32/32 and 34/34, all 46 non-live Python scripts and six fixed Node suites pass, Python compilation and eight JavaScript syntax checks pass, and protected protocol/policy/runtime hashes are unchanged. No live process, new authorization, skill record, capability evidence, or M4 evidence was created by this repair.
- Pushed the live root-graph repair as `2d3f37c0`, then pushed one new parent-bound authorization as `91d1c8cd` for episode `sp002_live_20260718_180956_cc91ef82`. The fixture re-audited exactly, the same-model zero-retry probe returned the expected 11-byte JSON in 2.662 seconds, and the authorization was consumed once. Session `dcdb63d5-a9c` made two successful zero-retry real Planner transports. The root response copied the exact two-node graph and one craft action; Mineflayer executed one accepted backend attempt and the stable terminal observation contains one real stone pickaxe. Cobblestone changed by `-3`, but stick changed by `0`, so exact machine verification rejected `stick_consumption_exact`. The post-action continuation also returned `planning` with `actions=[]`, preserving `planning_action_count_must_equal_one`; the run ended after 9.282 seconds with one successful action but no evidence credit. Seventeen credential-clean artifacts retain the result, Paper/Bridge stopped, ports cleared, server properties restored, and no retry or replacement authorization occurred.
- Replayed the second live failure through both client and persisted server state. Paper playerdata hash `2c34cb65...` contains no cobblestone or sticks and exactly one stone pickaxe, proving exact server-side recipe consumption; Mineflayer's crafting plugin closed the table and copied stale player slots into `bot.inventory`. The offline fix reopens the same table once with a 1.5-second bound, waits for authoritative `window_items`, closes it, records signed inventory deltas, and keeps the craft-attempt limit at one. The isolated SP-002 Agent now reconciles `verify_inputs` before action binding and strictly terminates only on the exact refreshed transition, so no continuation call is needed; terminal prompt rules still fail closed if one occurs. SP-002 is 18/18, existing runtime is 32/32, protocol is 34/34, all 46 non-live Python suites and six Node suites pass, and frozen shared Agent/SP-001 identities remain byte-valid. No live process, authorization, evidence credit, skill promotion, capability change, or M4 change occurred in this phase.
- Pushed the authoritative-inventory repair as `6389bd48`, then pushed parent-bound authorization `adb7c679` for episode `sp002_live_20260718_193656_a6ce837e`. The fixture tree re-audited exactly, the same-model probe returned exact 11-byte JSON in 2.108 seconds with one request and zero retries, and the authorization was consumed once. Session `e6cebfbe-dfe` made one schema-valid real Planner call, copied the exact two-node graph, and executed one accepted Mineflayer craft. The authoritative refresh proved exact `-3 cobblestone`, `-2 stick`, and `+1 stone_pickaxe`; both task nodes completed and the goal terminated in one cycle after 5.453 seconds. Machine verification and evidence eligibility pass with no criteria issues, 17 artifacts are hash-bound, Paper/Bridge stopped, ports cleared, and server properties restored. SP-002 advances to 1/3; no candidate, SP-003 action, capability evidence, M4 evidence, or automatic retry was created.
- Pushed the first-success evidence as `71edd812`, then pushed parent-bound authorization `2ba0c7db` for episode `sp002_live_20260718_201122_e277e1cb`. The fixture tree re-audited exactly, and the same-model probe returned exact 11-byte JSON in 1.875 seconds with one request, zero retries, and zero reasoning bytes. Session `009dafd7-659` made one schema-valid real Planner call and one accepted Mineflayer craft. The authoritative refresh again proved exact `-3 cobblestone`, `-2 stick`, and `+1 stone_pickaxe`; both task nodes completed in one cycle after 5.515 seconds. Machine verification and evidence eligibility pass with no issues, 17 artifacts are hash-bound, Paper/Bridge stopped, ports cleared, and server properties restored. SP-002 advances to 2/3; no candidate, SP-003 action, capability evidence, M4 evidence, or automatic retry was created.
- Pushed the second-success evidence as `7fcb7fd1`, then pushed parent-bound authorization `452d057a` for episode `sp002_live_20260718_203340_c9749b39`. The fixture tree re-audited exactly, host memory and controlled process gates passed, and the same-model probe returned exact 11-byte JSON in 2.077 seconds with one request, zero retries, and zero reasoning bytes. Session `102aa710-b41` made one schema-valid real Planner call and one accepted Mineflayer craft. The authoritative refresh again proved exact `-3 cobblestone`, `-2 stick`, and `+1 stone_pickaxe`; both task nodes completed in one cycle after 6.250 seconds. Machine verification and evidence eligibility pass with no issues, 17 artifacts are hash-bound, Paper/Bridge stopped, ports cleared, and server properties restored. SP-002 reaches 3/3 and unlocks separate offline candidate extraction; no skill record, SP-003 action, capability evidence, M4 evidence, or automatic retry was created.
- Pushed the third-success evidence as `05b6c1fb`, then reproduced the candidate-source gate offline. The builder previously required SP-001's exact ten-file shape and rejected all three valid 17-file SP-002 bundles; the lifecycle CLI also accepted only SP-001. Phase 58 pins task-specific 10/17 evidence counts, rechecks SP-002's exact material transition, one-call Planner controls, and 2/2 task graph, and permits only candidate/advisory commands for SP-002. Preview candidate `a488cd61` matches the fixed craft contract and reaches advisory-ready without mutating the real queue or skill library; executable promotion remains rejected. Protocol tests advance to 36/36 with no protocol-byte change and no live process.
- Pushed the Phase 58 tooling fix as `48000e10`, then ran the real offline extraction exactly once. Candidate `a488cd61` was appended to the five-record queue with three distinct episode/session/hash sources and an exact fixed craft contract; the learning ledger and `craft_stone_pickaxe_promotion.json` record `candidate/pending` and advisory readiness. The ten-record custom skill library remains byte-identical, so no advisory or executable skill was created. SP-003, normal runtime, capability, M4, live execution, and automatic retry remain locked.
- Pushed candidate extraction as `eba9c82f`, then applied the explicit SP-002 advisory review exactly once. Candidate `a488cd61` is now `advisory/approved`; `learned:craft_stone_pickaxe@1.0.0` was appended as the eleventh custom-skill record with the same three source sessions and canonical record hash `7c9a596e...`. Existing skill records were not rewritten. Normal runtime, executable craft promotion, SP-003, capability, M4, live execution, and automatic retry remain disabled while the paired-evaluation policy is defined.
- Defined `stone-pickaxe-sp002-paired-evaluation-v1` without starting Minecraft. It hash-binds the three immutable skill-off SP-002 successes as baseline `r1..r3`, requires live shadow/advisory/fallback support plus three exact candidate pairs, and gives every arm a separate parent-bound one-use authorization with no retry. The initial report is 0/3 and `retain_advisory`; 11/11 dedicated cases, 54/54 existing SP-002/protocol cases, all 62 frozen SP-001 evaluation cases, policy identity, compilation, JSON parsing, credential scan, and diff checks pass. New byte-preservation rules are isolated below the frozen SP-001 attributes. No live arm, normal runtime, capability, M4, or SP-003 permission exists before this definition commit is pushed.
- Pushed the v1 definition as `194a4e7d` and its single-file `shadow/shadow-1` authorization as `72f62dd4`, then consumed exactly that one live arm. Minecraft completed the exact craft in one Planner call and one Mineflayer action with machine verification passing, but the evaluation failed closed because `skill_shadow_plan_count=0`. The retained session proves a first-cycle ordering defect: `require_llm_root_plan` defers learned-skill routing until a valid root plan exists, while SP-002's root plan completes its only action in that same cycle. No retry occurred; v1 `shadow-1` is permanently consumed, the advisory skill remains nonexecutable, and a non-overlapping v2 recovery window is required before any further live arm.
- Defined isolated SP-002 recovery window v2 without starting Minecraft. The adapter preserves the LLM root plan, moves support routing into the first cycle, and uses policy-derived support IDs in its report. Policy `6222ba05...` binds the immutable v1 failure and fresh `shadow-2/advisory-2/fallback-2/r4/r5/r6` IDs. The generated baseline remains 3/3; the generated paired report remains 0/3 and `retain_advisory`. Dedicated v2 tests pass 15/15 and combined v1/v2 tests pass 26/26. No authorization exists until this definition is committed and pushed.
- Pushed the v2 definition as `47191b5b` and its single-file `shadow/shadow-2` authorization as `9f700994`, then consumed exactly that arm. Episode `sp002_skill_shadow_v2_20260719_005650_445ae601` and session `f2a47fbd-a79` machine-verified the exact craft in one Planner call and one Mineflayer action. `skill_shadow_plan` event 6 preceded first-cycle routing event 7 and real Planner call event 9; the run recorded one shadow plan, zero skill selections/executions, zero action failures/rejects/loops, and passed every evaluation check. The v2 report now has shadow support 1/1, total support 1/3, candidate pairs 0/3, and remains `retain_advisory`. No retry occurred and `shadow-2` is permanently consumed.
- Pushed `shadow-2` evidence as `702c413b` and its single-file `advisory/advisory-2` authorization as `c59603ab`, then consumed exactly that arm. Episode `sp002_skill_advisory_v2_20260719_011722_b2194e7a` and session `224e9a06-b32` machine-verified the exact craft in one Planner call and one Mineflayer action. `skill_advisory_hint` event 6 preceded first-cycle routing event 7 and real Planner call event 9; the run recorded one advisory hint, zero skill selections/executions, zero failures/rejects/loops, and passed every check. V2 support is now 2/3, candidates remain 0/3, and `fallback-2` remains unauthorized until this evidence is pushed.
- Pushed `advisory-2` evidence as `27f292df` and its single-file `fallback/fallback-2` authorization as `838ebbfb`, then consumed exactly that arm. Episode `sp002_skill_fallback_v2_20260719_013606_fa84d271` and session `d6b23162-dde` machine-verified the exact craft in one Planner call and one Mineflayer action. `skill_fallback` event 6 preceded first-cycle routing event 7 and real Planner call event 9; the ordinary Planner completed the goal while the advisory skill recorded zero selections and executions. All checks passed with zero failures, rejects, loops, or retries. V2 support is now 3/3 and candidates remain 0/3; `candidate/r4` requires a separate parent-bound authorization after this evidence commit is pushed. Normal runtime and SP-003 remain disabled.
- Pushed `fallback-2` evidence as `29c2c777` and its single-file `candidate/r4` authorization as `e050cbdc`, then consumed exactly that arm. Episode `sp002_skill_candidate_v2_20260719_015614_60dfbbb7` and session `45f20f79-0ac` preserved one real LLM root plan, selected and executed the exact learned craft once, and machine-verified the result in one action. Fixed controls and initial state match baseline `e6cebfbe-dfe`; candidate steps were verified and reobserved with attribution confidence 1.0, zero failures/rejects/fallbacks/loops, and no retry. V2 candidates advance to 1/3; `candidate/r5` remains unauthorized until this evidence commit is pushed.
- Pushed `r4` evidence as `26ebf9b4` and its single-file `candidate/r5` authorization as `2b9ea2d3`, then consumed exactly that arm. Episode `sp002_skill_candidate_v2_20260719_020805_7e04aab8` and session `6acf4503-737` preserved one real LLM root plan, selected and executed the exact learned craft once, and machine-verified the result in one action. Fixed controls and initial state match baseline `009dafd7-659`; candidate steps were verified and reobserved with attribution confidence 1.0, zero failures/rejects/fallbacks/loops, and no retry. V2 candidates advance to 2/3; `candidate/r6` remains unauthorized until this evidence commit is pushed.
- Pushed `r5` evidence as `76753f9e` and its single-file `candidate/r6` authorization as `06392a64`, then consumed exactly that arm. Episode `sp002_skill_candidate_v2_20260719_021828_219d1851` and session `2b6baf3a-e3e` preserved one real LLM root plan, selected and executed the exact learned craft once, and machine-verified the result in one action. Fixed controls and initial state match baseline `102aa710-b41`; candidate steps were verified and reobserved with attribution confidence 1.0, zero failures/rejects/fallbacks/loops, and no retry. V2 support and candidates are both 3/3, the report is `review_executable_new_version` with approved readiness, and the one-use external launcher is removed. Normal runtime and SP-003 remain disabled pending a separate offline 1.0.1 promotion review.
- Generalized the append-only executable promotion command for SP-002 without applying it. The task-specific gate rebuilds the exact v2 report, binds policy `6222ba05...`, rejects tampering before any write, preserves advisory `1.0.0`, appends only target `1.0.1`, and is idempotent. Focused promotion tests pass 7/7, the combined promotion/protocol suite passes 43/43, and the full stone-pickaxe regression passes 181/181. The real skill library, learning ledger, promotion artifact, and runtime-default gate remain unchanged until this tooling commit is pushed; normal runtime and SP-003 are still disabled.
- Pushed the promotion tooling as `abebe3a8`, then ran the exact SP-002 promotion command offline. It preserved advisory `learned:craft_stone_pickaxe@1.0.0` at canonical hash `7c9a596e...` and appended executable `1.0.1` at `c7f4e536...` with parent and rollback version `1.0.0`. Promotion artifact `36c4613b...` and approved runtime gate `3c8606be...` bind fingerprint `47840e03...`; a second real invocation changed no file hash, and runtime selection remains blocked until that gate is loaded, then selects exactly `1.0.1`. Post-promotion stone-pickaxe regression passes 181/181 and M1/M2/GoalVerifier/capability regression passes 85/85. No Minecraft process ran. SP-003 is unlocked only for protocol definition and offline verification after this promotion commit is pushed; live execution still requires a separate gate.
- Phase 72 defines `stone-pickaxe-sp003-empty-hand-runtime-v1` without starting Minecraft. The strict Agent path starts from exact empty inventory, creates the exact five-node graph, tracks three same-family log removals, one table craft/place, one wooden-pickaxe craft/equip, three tool-proven stone removals, and one final stone-pickaxe craft. It can return to the same machine-proven table, rejects iron and duplicate mutations, keeps the skill store immutable, and embeds the frozen SP-001/SP-002 verifiers. Baseline uses no skills; candidate routing is restricted to acquire `1.1.0` and craft `1.0.1`. Dedicated tests pass 19/19, all stone runtime/protocol tests pass 105/105, TaskSystem passes 106/106, the repository-wide non-live Python gate passes 955/955, M1/M2/GoalVerifier/capability passes 54/54, M3 skill regressions pass 21/21, and the M4 reset-only Node suite passes all 12 cases. No live authorization exists until this definition is committed and pushed.
- Phase 73 consumes baseline authorization `e5b8050b` exactly once. Paper and Bridge start a fresh seed-12345 world, BM-012 reset succeeds with exact empty inventory, no fixture blocks, and natural time 67, but the supplemental audit checks an absent `expected.initial_blocks` field and exact tick zero. It fails before Agent connection with zero LLM calls and zero actions. The six-file retained failure bundle is pushed at `402c0ea9`; no retry occurs.
- Phase 74 replays that immutable reset and repairs only its interpretation: no fixture now requires machine-observed `fixture_blocks=[]` plus `fixture.block=air`, while daylight start uses the established 600-tick M4 startup window with bridge time checks. Tampered fixture and 601-tick controls fail closed. Dedicated tests pass 20/20, fixed stone runtime/protocol tests pass 106/106, the repository-wide non-live Python gate passes 956/956, and all 12 M4 Node cases pass. A fresh baseline still requires a separate pushed authorization.
- Phase 75 consumes a new baseline authorization after the reset fix. Reset and Agent connection pass, then the Runner calls nonexistent `runtime_default_gate_profile()` before writing preflight. Session `2e34110a-808` contains two lifecycle events, zero Planner calls, zero skill selections, zero GoalVerifier calls, and zero actions. The eight-file failure bundle is pushed at `d18ae10c`; no retry occurs.
- Phase 76 replaces only that call with the existing public `skill_runtime_default_profile()` API and binds the exact two approved candidate skill names in tests. SP-003 remains 20/20 and fixed stone runtime/protocol remains 106/106. A fresh baseline still requires its own pushed authorization.
- Phase 77 consumes the third baseline authorization and runs the real Agent for the full 300-second bound. It removes three distinct dark-oak logs and performs one backend-successful, zero-retry craft from exactly three logs to exactly 12 matching planks. The generic pre-action verifier labels that recipe `review`, so the SP-003 progress recorder leaves `plank_craft_count=0`; eleven subsequent exact stick crafts are rejected against stale history. The complete 13-file manifest is hash-valid and pushed at `ec0b612d`; the run has no skill selection, no retry, no capability/M4 credit, and remains a retained failure.
- Phase 78 accepts `review` only for SP-003 craft reconciliation and still requires exact machine proof: matching result item/count/requested count, one backend attempt, zero retries, and exact positive inventory and signed deltas. The retained 12-plank transition now advances progress and unblocks the exact stick craft, while a tampered output delta fails closed. Dedicated tests pass 22/22, fixed stone runtime/protocol tests pass 108/108, the repository-wide non-live Python gate passes 958/958, Python compilation and the benchmark-reset Node suite pass, and frozen policy/protocol/evidence hashes remain unchanged. A fresh baseline still requires a separate pushed authorization.
- Phase 79 consumes the fourth baseline authorization exactly once. Episode `sp003_baseline_20260719_055541_f863c62c` and session `608f66a1-625` make 24 real Planner calls and 23 actions, acquire three logs, place the table, craft and equip one wooden pickaxe, and remove exactly three stone. The return move begins with `cobblestone:3,dirt:3` and ends with neither item; a cobblestone block is machine-observed at `(94,141,-31)`. Installed Pathfinder 2.4.5 defaults permit digging and one-block towers with dirt/cobblestone scaffolding, so the successful move consumed the final recipe inputs outside the Planner-selected action. The 13-file failure bundle is immutable and pushed at `3bd58c38`; no retry, skill credit, capability credit, or M4 credit occurs.
- Phase 80 adds an SP-003-only Node preload that disables Pathfinder digging, one-block towers, and scaffolding without changing the frozen shared bridge. Guarded moves are horizontal and inventory-preserving; every such move receives one post-action machine observation and fails closed on any inventory loss. The bounded repair also normalizes table reference coordinates, navigates before distant stone digs, excludes support-column stone, and retains multiple local stone candidates. Dedicated tests pass 25/25, fixed stone suites pass 111/111, the repository-wide non-live Python gate passes 961/961, and seven Node suites pass all 58 internal cases. Policy, protocol, shared bridge, and retained evidence hashes remain unchanged. A fresh baseline still requires a separate pushed authorization.
- Phase 81 pushes one fresh parent-bound baseline authorization at `c693c07b` for episode `sp003_baseline_20260719_070034_8d6e8eef`. Its audit passes every identity and scope check: baseline skill mode is off, automatic retry and batching are false, and capability/M4 credit are false.
- Phase 82 consumes that authorization on one controlled startup attempt. Paper creates the fresh world and reports ready, but Node 22 treats bare preload argument `src/bot/sp003_inventory_preserving_navigation.js` as a package specifier and exits with `MODULE_NOT_FOUND` before bot connection. The launcher cleans both ports, restores server properties, and does not start the Agent runner. The retained five-file infrastructure bundle records zero sessions, LLM calls, actions, skill selections, or retries. The authorization and world cannot be reused; the next gate is a pushed one-line `./src/...` module-specifier repair, followed by a separate authorization.
- Phase 83 changes only the launcher argument to `./src/bot/sp003_inventory_preserving_navigation.js` and updates its exact test binding. A real Node 22 preload smoke resolves the module, instantiates the patched `Movements`, and proves `canDig=false`, `allow1by1towers=false`, and an empty scaffolding list. The preload implementation and shared bridge remain byte-identical. SP-003 passes 25/25, fixed stone suites pass 111/111, the repository-wide non-live Python gate passes 961/961, and seven Node suites pass all 58 internal cases. A fresh baseline still requires a separate pushed authorization.
- Phase 84 pushes one fresh parent-bound baseline authorization at `5b32ee08` for episode `sp003_baseline_20260719_072552_4e3a282c`. Its audit binds the Phase 83 repair, baseline skill mode off, no retry, and no capability or M4 credit.
- Phase 85 consumes that authorization exactly once. The Agent starts at `(96.5,144,-31.5)` on `dark_oak_leaves`, makes 10 real Planner calls, removes 10 observed logs, and detects all 10 drops, but every pickup path fails because non-mutating Pathfinder cannot descend from the canopy. The terminal inventory remains empty, no action succeeds, and no table, wooden-pickaxe, stone, or stone-pickaxe transition occurs. The 13-file evidence bundle is retained with manifest `fecc6ad9...`; the next gate is an offline observation-bound canopy-egress repair, not another live run.
- Phase 86 replays the immutable initial observation and selects `grass_block:121:141:-36` as the nearest eligible ground egress, with exact clear stand cell `(121,142,-36)`. The target is navigation-only, at least eight horizontal blocks away, below the player, and admitted only when both stand and head cells are clear in the same observation. Direct canopy log digs, unobserved targets, and missing or obstructed egress candidates fail closed. The existing preload still forbids digging, towers, and scaffolding, and every move remains inventory-preserving. SP-003 passes 27/27, fixed stone suites pass 113/113, the full Python repository passes 963/963, and seven Node suites pass 58/58 internal cases. No live process ran; a fresh baseline still requires a separate pushed authorization after this repair is pushed.
- Phase 87 pushes one fresh parent-bound baseline authorization at `9b3ae9f8` for episode `sp003_baseline_20260719_081246_b6ebff81`. It binds Phase 86 commit `b6ebff81`, baseline skill mode off, one episode, no retry, and no capability or M4 credit.
- Phase 88 consumes that authorization exactly once. The navigation-only canopy target succeeds, the Agent gathers three oak logs, crafts exact planks and sticks, crafts and places one table, and crafts/equips one wooden pickaxe. It then removes three stone blocks whose drops are three vertical blocks below the surface player; all three pickups fail under the non-mutating Pathfinder. Five farther direct digs fail at the guard and eleven approach moves stop without mutation. The terminal inventory is `stick:2, oak_planks:3, wooden_pickaxe:1`, with zero cobblestone and no stone pickaxe. The 13-file evidence bundle is retained under manifest `2d354144...`; the next gate is an offline observation-bound grounded stone approach, not another live run.
- Phase 89 replays the retained `stone:124:139:-37` geometry and offline-verifies a bounded two-stage approach. The first target is a horizontal clearance probe with tolerance one. A later radius-one scan proves all 63 cells only when its response is below the frozen backend's 50-result limit and the player cell is stable; exact clear cells then bind stand `(124,140,-37)` above the still-unmined stone. Direct probe/anchor digs, truncated scans, obstruction, forged proof, and position drift fail closed. Once grounded, the support stone is excluded and an adjacent same-level stone is diggable. SP-003 passes 30/30, all stone suites 211/211, full Python 966/966, and Node 58/58. No live process ran and no capability state changed; the next gate is to push this repair, then create one separate fresh parent-bound baseline authorization.
- Phase 90 pushes one fresh parent-bound baseline authorization at `60fda7b9` for episode `sp003_baseline_20260719_092557_f63c1161`. It binds Phase 89 commit `f63c1161`, baseline skill mode off, one episode, no retry, and no capability or M4 credit.
- Phase 91 consumes that authorization exactly once. The Agent again completes three logs, exact planks and sticks, one placed table, and an equipped wooden pickaxe. Its first wooden-pickaxe craft rolls back before a later craft succeeds, independently violating the single-attempt criterion. The Phase 89 horizontal probe then reaches `(123.5,142,-37.5)` and reconstructs a complete 29-non-air-cell local scan, but both exact stone supports at `(124,139,-38)` and `(124,139,-37)` have dirt in both stand and head cells. No approach proof is emitted, twelve unproven stone digs fail at the guard, zero stone is removed, and the 300-second deadline ends the run. The 13-file evidence bundle is retained under manifest `323689c2...`; no retry ran and the next gate is an offline craft-settlement plus bounded surface-clearance repair.
- Phase 92 evolves only the supplemental SP-003 policy to runtime-v2. Its Node preload wraps process-local `createBot` so a resolved interactive craft settles for 1000ms before the frozen bridge refreshes table inventory; the original craft is called once, failures propagate, and no backend retry is added. The Python runtime requires a stable complete 63-cell scan below the frozen 50-result cap, binds exact stone support and scan fingerprint, permits only `grass_block`/`dirt`, chooses the nearest support and highest obstruction, caps the whole episode at three removals, and requires the full shaft clear before descent. The retained 29-block Phase 91 replay selects `grass_block:124:142:-38`, `dirt:124:141:-38`, and `dirt:124:140:-38`, then proves stand `(124,140,-38)`. SP-003 passes 33/33, all stone suites 214/214, full Python 969/969, and all seven Node suites with 61 internal cases. Shared `bot_server.js`, the base protocol, skills, capability state, and retained evidence are unchanged; no live process ran and the next gate is a pushed offline-fix commit followed by one fresh parent-bound baseline authorization.
- Phase 93 consumed exactly one parent-bound baseline authorization after Phase 92 was pushed. Paper created the fresh seed-12345 world and became ready, but Bridge exited before bot readiness because the process-local preload called `wrapCraftSettlement` synchronously while Mineflayer had not yet emitted `inject_allowed` or injected `bot.craft`. The retained manifest `6389aef1...` binds normalized authorization, Bridge stack trace, server startup log, and failure record. Agent episode, session, LLM calls, skill selections, and gameplay actions are all zero; automatic retry is false. The next gate is to push this immutable infrastructure evidence, then defer only the process-local craft wrapper until internal plugin injection.
- Phase 94 changes only the SP-003 process-local preload and its exact supplemental policy binding. `createBot` returns its bot without synchronously requiring `craft`; one listener installs `sp003CraftWithSettlement` after Mineflayer's earlier internal `inject_allowed` listener injects plugins. A real Mineflayer loader smoke observes `craft=undefined` at return and the wrapped function after injection. Duplicate installation, missing post-injection craft, eager policy installation, synchronous-craft policy tampering, hidden retry, and failure-delay cases all fail closed. SP-003 passes 34/34, all stone suites 215/215, full Python 970/970, and all seven Node suites with 64 internal cases. Shared `bot_server.js`, the frozen protocol, and Phase 93 evidence remain unchanged; no live process ran.
- Phase 95 consumes exactly one fresh parent-bound baseline authorization after Phase 94 was pushed. Paper, Bridge, Mineflayer plugin injection, and the Agent all start successfully, live-proving the deferred wrapper installation. The first exact oak log is removed and its drop entity is detected, but immediate pickup ends without inventory delta. The second exact log action then gains two oak logs, proving the delayed first drop arrived together with the current drop; a third exact log adds one more. Terminal inventory is `oak_log:3`, while progress contains only the latter two source IDs. Eight subsequent plank crafts fail closed at the `acquire_wood` guard and the 300-second deadline ends the run. The immutable 13-file evidence bundle is retained under manifest `c1b22e69...`; no retry, skill selection, capability credit, or M4 credit occurred.
- Phase 96 adds only SP-003 process-local progress and evidence reconciliation under `sp003-delayed-log-pickup-reconciliation-v1`. A failed log action is pending only when accepted verification, exact source coordinates, exact log-to-air mutation, exact expected drop, a detected but ungrounded same-item pickup attempt, zero immediate inventory delta, and the original backend error all agree. A later same-family successful dig reserves one item for its own source and may reconcile only the oldest fingerprinted pending sources covered by its exact inventory surplus, never exceeding three total sources. The raw failure remains in `raw_action_failures`; only its separately proven index leaves `unreconciled_action_failures`. The retained Phase 95 replay advances `0 -> 2 -> 3` exact sources with fingerprints `aeead701...` and `b9afa454...`. Wrong family, absent surplus, malformed mutation, missing entity, duplicate replay, over-capacity, and verifier tampering fail closed. Audit `0041fd9d...` binds the repair; shared Bridge and base protocol hashes are unchanged, no live process ran, and no capability state changed.
- Phase 97 consumes exactly one fresh Phase 96 parent-bound baseline authorization. Episode `sp003_baseline_20260719_121946_1d855e28` and session `3c66112c-4c1` complete the empty-hand chain through a placed table and equipped wooden pickaxe, then remove three bounded surface-clearance blocks and machine-prove one stone pickup. The next stone at `stone:125:139:-37` is removed and its cobblestone entity `1021` is detected at `(125.125,139,-36.792725)`, but direct `GoalNear(1)` resolves at player position `(124.434122,140,-36.488775)` without inventory delta. The strict three-dimensional distance remains `1.252876`, while no standable fallback exists within the fixed margin. Progress remains one exact stone, later clearance and stone requests fail closed, and the 300-second deadline ends the run. Manifest `bd452cd4...` binds all 12 payload files; no retry, skill selection, capability credit, or M4 credit occurs. The next gate is an offline fix for the direct-navigation completion metric mismatch, not another live authorization.
- Phase 98 keeps the shared Bridge at frozen SHA-256 `f1677b32...` and changes only the SP-003 preload, runtime guard, policy, and tests. The preload maps process-local three-dimensional `GoalNear(1)` to exact range 0 while leaving non-unit ranges and `GoalNearXZ` unchanged. Stone-approach moves retain Pathfinder range 1 but use cell-center coordinates and a mathematically bounded 1.6 continuous tolerance; Phase 97's failed endpoint replays at distance `1.093800`. A table request is repaired at most once only when its requested solid is observed but unsafe, selecting the nearest clear observed reference; arbitrary coordinates still reject. SP-003 passes 48/48, all stone suites 231/231, full Python 984/984, and seven Node suites with 67 internal cases. Audit `0f5df8f9...` binds the fix. No live process, retry, baseline credit, capability credit, or M4 credit occurs; the next gate is commit/push followed by one fresh parent-bound baseline authorization.
- Phase 99 pushes Phase 98 as `4bde0e15`, then consumes exactly one separately pushed parent-bound authorization `0283c1f9` for episode `sp003_baseline_20260719_135031_9aa6c664`. Paper, Bridge, Mineflayer, reset, and fixed preflight pass. The sole root call to fixed model `deepseek-v4-flash` returns `InternalServerError -> HTTPStatusError` with HTTP 500, zero response bytes, one transport attempt, and zero retries. The Agent creates zero tasks, executes zero actions, preserves exact empty inventory, and terminates `empty_plan` after 1.968 seconds. This external provider failure occurs before any Phase 98 table, movement, or pickup repair can execute, so it provides no baseline, skill, capability, or M4 credit. Thirteen immutable files are retained under manifest `2900cccf...`; a dedicated replay verifies fail-closed behavior and every evidence hash. The next authorization remains false until this evidence commit is pushed, after which any new baseline requires a separate fresh parent-bound transaction and re-established provider health.
- Phase 100 starts no Minecraft process and sends exactly one fixed-provider health request after Phase 99 evidence is synchronized at `812d2e64`. The request uses the protocol endpoint and `deepseek-v4-flash`, temperature 0, max tokens 4096, JSON-object response mode, disabled thinking, a 30-second timeout, and zero SDK retries. It returns exact 11-byte `{"ok":true}` with historical response SHA-256 `4062edaf...`, stop finish reason, zero reasoning bytes, and 50 total tokens in 1.969 seconds. Credential values and response identifiers are not retained. Artifact `8cb3fc4c...` and its fixed-control regression pass; SP-003 is 50/50, stone-prefixed Python is 231/231, and full Python is 986/986. No authorization, world process, baseline credit, capability credit, or M4 credit occurs; the next gate is this health-proof commit/push followed by a separate authorization-only commit.
- Phase 101 consumes exactly one fresh authorization from pushed commit `59fc01e`. The 191.984-second baseline executes 19 actions and machine-proves three logs, one plank craft, one stick craft, one crafted and placed table, one crafted and equipped wooden pickaxe, three bounded surface clearances, and one distinct stone pickup. Planner calls 6 and 7 nevertheless request `oak_planks:2` after the stick stage, so the strict guard records two unreconciled failures; one pre-equip clearance request adds a third. The terminal compact state then exposes 2,031 progress characters plus 11,687 target characters containing seven full clearance proofs. Prompt tokens jump from 3,803 to 7,512, and call 19 is rejected only on `reasoning_too_long`, ending at `empty_plan` with `cobblestone:1`. The 13-file bundle is immutable, no retry ran, and no baseline, skill, capability, or M4 credit is granted. Its retained replay passes within SP-003 `51/51`, all stone-pickaxe `232/232`, and full Python `987/987`. The next gate is a bounded offline planner-state compaction with an explicit machine stage, not another live authorization.
- Phase 102 implements that offline repair without rewriting planner output into scripted actions. The planner receives only 16 monotonic progress fields, one of 15 allowlisted target fields, and no clearance-proof body; it computes 13 exact stages from current inventory, equipment, observed table distance, and verified progress. Retained call 6 is grounded to `craft_crafting_table`, where exact table craft passes the unchanged guard and stale `oak_planks:2` still fails. Retained call 19 is grounded to `acquire_cobblestone`; compact JSON is 1,614 characters and the user prompt is 2,724 characters. The 320-character reasoning limit remains fail-closed. Audit `686b7eca...` binds the source, policy, replay, protocol, Bridge, and Probe 21/22 hashes. SP-003 is 69/69 and all stone-pickaxe tests are 250/250; no Minecraft process, retry, baseline credit, capability credit, or M4 credit occurs.
- Phase 103 consumes exactly one authorization from pushed commit `b0f578c2`. Episode `sp003_baseline_20260719_154642_73c81d37` makes 13 real fixed-model calls, each with one transport attempt and zero retries. Calls 0-11 are schema-valid; 12 emitted actions all pass machine verification: three moves, three distinct log digs, four one-attempt crafts, one table placement, and one wooden-pickaxe equip. Terminal inventory is exact `stick:2, oak_planks:3, wooden_pickaxe:1`; surface-clearance and stone removal counts are zero. Calls 10-12 remain correctly staged at `acquire_cobblestone` with bounded prompts. Call 12 returns 523 bytes under `finish_reason=stop` but fails only `action_parameter_y_invalid`: target `stone:124:139:-37` is a navigation-only clearance probe, the prompt requires `move_to` with only `x/z`, and the generic envelope validator requires `x/y/z`. The Agent stops at `empty_plan` after 79.172 seconds. Manifest `fcce5b84...` binds the 13-file ineligible bundle; no retry, baseline, skill, capability, or M4 credit is granted. The next gate is evidence push followed by an offline mode-specific schema repair, not another live authorization.
- Phase 104 implements that repair under supplemental policy `sp003-horizontal-move-envelope-v1`. In strict SP-003 mode, `move_to` now requires finite `x/z`; `y` is optional but must be finite when present. SP-003 `look_at`, SP-001, SP-002, and generic stone modes retain mandatory finite `x/y/z`. The retained call-12 observation reconstructs as a valid horizontal move, while invalid optional `y` and every out-of-scope omission fail closed. The prompt, runtime action guard, shared Bridge, frozen protocol, and all retained evidence are unchanged; Planner output is never rewritten. Audit `649dc406...` binds current source/policy identities and immutable Phase 103 evidence. SP-003 is 72/72, all stone-pickaxe tests are 253/253, and full Python is 1008/1008; no live process, retry, baseline, skill, capability, or M4 credit occurs.
- Phase 105 consumes exactly one fresh authorization from pushed commit `f1cb908f`. Episode `sp003_baseline_20260719_164341_b6f52e23` makes 27 schema-valid Planner calls with one transport attempt each and zero retries, then completes three logs, exact planks and sticks, one crafted and placed table, and one crafted and equipped wooden pickaxe. It removes three bounded surface blocks, but its first two covered-stone actions remove the stone without collecting either cobblestone drop. Immutable geometry places dirt in each post-dig head cell; direct pickup navigation stops at distances 1.683 and 1.165, and no standable fallback exists. The deadline ends the run with zero cobblestone and no stone pickaxe. The 13-file bundle remains ineligible, the authorization is consumed, and no retry occurs.
- Phase 106 implements `sp003-pre-dig-pickup-access-v1`. Every direct stone action now requires exact stone target/support cells, a target that becomes the stand cell after verified removal, and a machine-proven air head cell. Unsaturated scans retain complete 63-cell semantics; a frozen 50-result response is accepted only when its unique-name prefix, coordinate-bound distance order, strict final cutoff, full selection trace, and scan fingerprint can be independently reconstructed. Caller-supplied proofs are stripped and recomputed. Surface clearance remains bounded to three blocks per shaft and six per episode. Retained Phase 105 geometry now emits the missing head clearance before stone mutation, wrong-family supports and visibility tampering fail closed, and the shared Bridge remains at `f1677b32...`. Audit `465a39fc...` binds the repair. SP-003 passes 77/77, all stone-pickaxe tests pass 258/258, full Python passes 1013/1013, capability consistency passes 8/8, and seven Node suites pass 67/67 internal cases. No live process, authorization, baseline, skill, capability, or M4 credit occurs.
- Phase 107 consumes exactly one authorization from pushed commit `44b971a7`. Episode `sp003_baseline_20260719_184317_c5963fb7` makes 33 schema-valid Planner calls with one transport attempt each and zero retries. Its first 18 actions complete three logs, exact planks and sticks, one crafted and placed table, one crafted and equipped wooden pickaxe, and four exact surface clearances. Action event 376 carries live `sp003-pre-dig-pickup-access-v1` proof `cbd7c37...`, removes stone `124,139,-38`, exposes air feet/head over stone support, and detects cobblestone entity 949. Direct `GoalNear` stops at distance 1.777; the fallback candidate is only 0.530 from the drop, yet `GoalBlock` resolves at the identical unchanged player position and remains outside acquisition range. Fourteen later actions are all navigation attempts, including six nearest-observed guard rejections, and the run ends `max_actions` with no cobblestone. Manifest `f4556504...` binds all 12 payload files; no retry, baseline, skill, capability, or M4 credit occurs. The next gate is evidence push followed by an offline SP-003-only goal-completion grounding repair.
- Phase 108 implements `sp003-goalblock-completion-grounding-v1` without modifying the shared Bridge, base protocol, or `mineflayer-pathfinder` dependency. The SP-003 preload wraps the process-local Pathfinder plugin once, preserves all non-`GoalBlock` behavior and original errors, and checks `goal.isEnd(player.position.floored())` after a nominal resolve. Recovery is allowed only from a grounded player into an adjacent cell exactly one level lower with solid support, passable feet/head, and center distance at most 1.6. It uses only look and forward controls for at most four 125ms pulses, rechecks geometry each pulse, and fails closed on missing controls, changed geometry, or exhaustion. The retained Phase 107 integrated dig replay follows the exact empty-path failure, reaches the proven fallback after two pulses, and observes `cobblestone:+1`; adverse geometry and no-progress controls reject. Audit `cc34b7e...` binds the repair. SP-003 passes 79/79, all stone-pickaxe tests 260/260, full Python 1015/1015, capability consistency 8/8, and seven Node suites 73/73. No live process, authorization, baseline, skill, capability, or M4 credit occurs.
- Phase 109 consumes exactly one authorization from pushed commit `4b5bfc25`. Episode `sp003_baseline_20260719_200026_f434442e` makes 33 schema-valid Planner calls with no retry and completes three logs, the table, the wooden pickaxe, its equip, and four surface clearances. Its last sixteen actions are all `move_to`: twelve return success without changing position because continuous distance `1.420553` is within Bridge tolerance `1.6`, although exact player cell `{124,141,-37}` differs from target `{124,140,-38}`; four alternatives fail nearest-target grounding. The run reaches 32 actions before any stone dig, and Phase 108 `GoalBlock` recovery is not exercised. The immutable 13-file failure bundle is pushed at `bc20b5a0`; no retry or capability credit occurs.
- Phase 110 implements `sp003-exact-goalnear-completion-grounding-v1` in the same process-local preload. It applies only to project-marked `GoalNear` instances transformed from requested range 1 to effective range 0, preserves explicit range 0, non-unit, unmarked, `GoalNearXZ`, and original-error behavior, and reuses the bounded clear down-step proof without dig, place, scaffold, or dependency modification. The exact Phase 109 geometry, integrated Bridge `move_to`, and direct pickup path close after two pulses; blocked-head and four-pulse exhaustion controls reject. Audit `db1372ee...` binds the repair. SP-003 passes 81/81, all stone-pickaxe tests 262/262, full Python 1017/1017, capability consistency 8/8, and seven Node suites 75/75. No live process, authorization, baseline, skill, capability, or M4 credit occurs.
- Phase 111 consumes exactly one authorization from pushed commit `e148ac11`. Episode `sp003_baseline_20260719_210605_35b1bb55` makes 33 schema-valid Planner calls with no retry and executes 32 actions. Its first log pickup navigation returns dependency timeout after six seconds, and Bridge calls `pathfinder.stop()`, which only sets a deferred flag. The next `goto` emits `PathStopped`; every catch calls `stop()` again, yielding 28 stopped results, including 25 failed horizontal moves at unchanged position `(119.456010,141,-33.597771)`. Two log actions succeed, three removed-log pickups remain pending, terminal inventory is `oak_log:2`, and no craft or stone action occurs. Phase 110 is not exercised. Manifest `e4e1eb83...` and all 13 files replay exactly; no retry, baseline, skill, capability, or M4 credit occurs.
- Phase 112 implements `sp003-pathfinder-stop-drain-v1` in the process-local preload without modifying the shared Bridge or vendored dependency. The wrapper invokes the original stop once, then immediately calls the original `setGoal(null)` to force the dependency's reset path to consume the deferred stop before return. A retained first timeout followed by a second navigation now succeeds with two total `goto` calls and zero retry; an active `goto` rejects rather than resolving falsely. Node covers exact call counts, idempotent installation, missing lifecycle APIs, Bridge dig-to-move integration, no added world mutation, and the real Mineflayer plugin lifecycle. Audit `406ebbab...` binds the repair. SP-003 is 83/83, all stone-pickaxe tests are 264/264, full Python is 1019/1019, and seven Node suites are 79/79. No live process, authorization, baseline, skill, capability, or M4 credit occurs.
- Phase 113 consumes exactly one authorization from pushed commit `4206aa17`. Episode `sp003_baseline_20260719_221125_16ea129e` completes the wood chain, places the table, crafts and equips the wooden pickaxe, clears four surface blocks, and removes stone `124,139,-38`; no `PathStopped` result recurs. Cobblestone entity 952 is detected, but exact-unit `GoalNear` recovery cannot cross from player cell `(124,140,-37)` into the lower target because dirt remains at transition upper-head cell `(124,141,-38)`. The pre-dig proof includes only `(124,140,-38)`, so eleven later moves retain grounding warnings while Bridge's continuous 1.6 tolerance reports success. The run ends `max_actions` with no cobblestone or stone pickaxe. Manifest `10c72888...` and all 13 files replay exactly; no retry, baseline, skill, capability, or M4 credit occurs.
- Phase 114 implements `sp003-downstep-transition-clearance-v1` within the SP-003 runtime and process-local preload. Complete and strict-visibility scans require the target `+2` transition upper-head cell for a one-level descent; the bounded shaft remains at most three blocks and clears top-down. Pickup and surface proofs advance to schema 2, success accounting and the independent verifier reject old or incomplete proofs, and exact-y pickup approaches use continuous tolerance 1.0 with failure reclassification forbidden. The retained Phase 113 action-18 state rejects direct stone, selects `dirt:124:141:-38`, and emits a valid stone-access proof only after derived air clearance. A blocked Node transition remains a Bridge failure rather than warning-success. Audit `2cd04b4a...`, SP-003 86/86, stone-pickaxe 267/267, full Python 1022/1022, selected regressions 243/243, and Node 80/80 pass. No live process, authorization, baseline, skill, capability, or M4 credit occurs.
- Phase 115 pushes the Phase 114 repair at `ae43ecbc`, then consumes exactly one separately pushed authorization `831a51b3` for episode `sp003_baseline_20260719_232840_66a67eeb`. All 33 Planner calls are schema-valid with one transport attempt and zero retries. Actions 17-18 clear the newly required transition column; actions 19-21 remove three distinct stones and ground three cobblestone pickups, leaving exact terminal materials `cobblestone:3, stick:2`. The earliest unrecovered transition is action 20: before it, `stone:124:139:-37` is the sole adjacent climbable support from player cell `(124,139,-38)`, with machine-visible air at its stand and head cells while all three alternatives are blocked by dirt. Mining that anchor and then descending on action 21 leaves the player enclosed at y=138. Actions 22-32 all fail to return to the table at unchanged position, so no stone-pickaxe craft occurs. Manifest `77a95b8e...` and all 13 files are retained; no retry or evidence credit occurs.
- Phase 116 adds SP-003-only policy `sp003-local-crafting-table-staging-v1` without changing the Phase 114-bound runtime, process-local preload, harness policy, shared Bridge, vendored Pathfinder, or base protocol. The overlay reuses the retained machine stone candidates before table placement: exact x/z probes preserve inventory, and only structured grass/dirt clearance proofs can authorize the same five bounded hand clearances. Once `stone:124:139:-38` has direct pickup access, the existing placement guard selects `grass_block:123:141:-37` and places the sole table at `(123,142,-37)`. The retained terminal player position is 4.297 blocks away, so both wood and stone pick crafts bind the same nearby table without any pit egress. The exact projected action count is 21/32. Audit `stone_pickaxe_sp003_local_table_staging_repair.json`, focused 11/11, stone-pickaxe 280/280, full Python 1034/1034, 1662 JSON files, credential scan, compilation, and seven Node suites pass. No Minecraft process, authorization, retry, baseline, skill, capability, or M4 credit occurs.
- Phase 117 pushes Phase 116 at `c5f120cc`, consumes exactly one separately pushed authorization `38e5c370`, and retains episode `sp003_baseline_20260720_011038_c6886c53`. The first seven actions complete movement, three distinct logs, twelve planks, four sticks, and one carried table. Actions 8-15 are rejected by the local staging guard: five use exact machine target `(121,137,-33)` but include `y`, while three use wrong `y=140`. The first x/z-only target appears at action 16 after more than 230 seconds and succeeds; action 17 also succeeds. The terminal state still carries the table with `oak_planks:6, stick:4`, and the run ends `max_duration` at 300 seconds with no post-deadline action, table placement, wooden pickaxe, stone, or stone pickaxe. Manifest `a7580b5b...` and all 13 files are retained; no retry or evidence credit occurs.
- Phase 118 adds SP-003-only policy `sp003-exact-navigation-parameter-normalization-v1` without modifying the frozen Phase 116 module, Planner, runner, base SP-003 runtime, process-local navigation preload, shared Bridge, vendored Pathfinder, harness policy, or protocol. The overlay strips only redundant y from an action whose key set is exactly `x,y,z` and whose coordinates match the current structured machine target within 0.01, then delegates to the existing Phase 116 center/tolerance/inventory binding. Five retained exact cases become one-attempt allowed moves; three wrong-y cases, a 0.02 near target, unexpected extra parameters, and skill context remain rejected. A new runner wrapper injects the overlay only in the SP-003 process, while the frozen runner hash remains `0ca5ad64...`. Audit `11100a3f...`, focused 7/7, cross-phase 19/19, stone-pickaxe 287/287, full Python 1042/1042, 1675 JSON files, compilation, credential scan, and seven Node suites pass. No Minecraft process, authorization, retry, baseline, skill, capability, or M4 credit occurs.
- Phase 119 pushes Phase 118 at `b77f7fe5`, consumes exactly one separately pushed authorization `9225f5e2`, and retains episode `sp003_baseline_20260720_015550_a54561d9`. Both exact xyz navigation actions are normalized and succeed, proving the Phase 118 boundary live. Actions 1-15 complete three logs, exact planks and sticks, one carried table, three moves, and six valid surface clearances with no rejection. The first clearance pickup moves the player from cell `(120,141,-37)` into its support column `(121,141,-37)`; that support becomes ineligible and the next action opens a second shaft. The six removals consequently spread across four supports as `1+1+3+1`. A complete terminal scan still exposes `grass_block:124:140:-38` as the sole remaining obstruction above `stone:124:139:-38`, but the unchanged episode maximum is exhausted. Action 16 `wait` is rejected because no bounded staging target remains, and the run ends `blocked_plan` at 115.453 seconds with no post-deadline action. Manifest `fb64e76f...` binds all thirteen files; no retry, baseline, skill, capability, or M4 credit occurs.
- Phase 120 adds the SP-003-only process-local overlay `sp003-partial-clearance-shaft-continuation-v1`. A lock is reconstructed only from successful clearance mutations with exact source/support geometry and SHA-256 proof fingerprints. In the retained first-displacement state, the old scheduler selects `stone:122:138:-36`; the overlay instead keeps `stone:121:138:-37` and machine-selects grounded egress `(120.5,141,-36.5)` over `grass_block:120:140:-37`. The same proof fingerprint permits one attempt, while a new verified clearance state may receive one fresh attempt. Synthetic replay clears one three-block shaft, reaches raw validated pickup access, and exposes a real table placement reference even when the base scheduler excludes stone directly below the player. The three-per-shaft and six-per-episode limits do not change, and a seventh clearance remains rejected. Audit `f7a762f9...`, focused 10/10, cross-phase 30/30, stone-pickaxe 298/298, TaskSystem/GoalVerifier/capability 140/140, selected M1/M2/M3 72/72, full Python 1053/1053, 1688 JSON files, compilation, credential scan, and seven Node suites with 80 internal cases pass. No Minecraft process, authorization, retry, baseline, skill, capability, or M4 credit occurs.
- Phase 121 pushes Phase 120 at `360aa2d8`, consumes exactly one separately pushed authorization `80bfb123`, and retains episode `sp003_baseline_20260720_030918_15498d1d`. All 14 Planner calls are real and schema-valid with one transport attempt and zero retries. Actions 1-12 succeed: three exact oak logs, twelve planks, four sticks, one carried table, four moves, and two machine-proven clearances. Guard cycle 12 selects `locked_surface_clearance` on the same `stone:124:139:-37` support chosen at cycle 11, live-validating the Phase 120 lock and eliminating the old multi-shaft fragmentation. The second dirt pickup enters cell `(124,141,-37)`. The terminal complete scan shows the remaining obstruction `dirt:124:140:-37`; every same-y cardinal feet cell is solid, but west has `grass_block` ground `(123,141,-37)` with air stand `(123,142,-37)` and air head `(123,143,-37)`. The horizontal-only egress proof returns unavailable, action 13 `wait` is rejected, and the run ends `blocked_plan` at 93.031 seconds without a post-deadline action. Manifest `f7f8cb57...` binds all thirteen files; no retry, baseline, skill, capability, or M4 credit occurs.
- Phase 122 adds SP-003-only process-local policy `sp003-partial-clearance-shaft-step-up-egress-v1` on the pushed Phase 121 evidence commit `5bd4210f`. It preserves Phase 120 same-level egress precedence and activates only for the exact `locked_partial_shaft_machine_egress_unavailable` blocker. Retained replay machine-selects west stand `(123.5,142,-36.5)` from complete scan proof: horizontal Manhattan distance one, vertical delta one, allowed solid ground, current and destination feet/head air, inventory preservation, no world mutation, and one attempt per proof fingerprint. Rebuilt post-step observation keeps support `stone:124:139:-37` locked and selects remaining `dirt:124:140:-37` for top-down clearance. Wrong y, extra parameters, skill context, malformed scans, blocked destination head, wrong ground family, and repeat attempts all reject. Audit `0b3ece89...`, focused 11/11 and Phase116-122 42/42 pass before the full regression sweep. No Minecraft process, authorization, retry, baseline, skill, capability, or M4 credit occurs.
- Phase 123 pushes Phase 122 at `6d9d1b5b`, consumes exactly one separate authorization `8dd14bfe`, and retains episode `sp003_baseline_20260720_040104_4676408a`. The Agent behaviorally completes the entire empty-hand chain for the first time: goal verification and all five task nodes pass, 23/24 actions succeed, three distinct stones produce three cobblestone, SP-002 passes, and the final stable inventory includes `wooden_pickaxe:1` plus `stone_pickaxe:1`. Strict SP-003 remains ineligible. The first table-bound wooden-pickaxe action invokes one backend craft call but returns no inventory delta after an authoritative window refresh; the next Planner cycle repeats it successfully against the same table. That leaves two wooden-pickaxe craft actions and one unreconciled failure. The integrated SP-001 component separately rejects `initial_movable` and nearest-source checks for transitions one and three. Phase 122's step-up branch is not exercised because the pickup path remains on the surface. Manifest `6420a734...` binds all thirteen files; no automatic retry, baseline, skill, capability, or M4 credit occurs.
- Phase 124 starts from pushed Phase 123 evidence commit `33441a18` and changes only the SP-003 process-local preload, its policy identity, focused tests, and audit bookkeeping. Policy `sp003-first-table-tool-craft-readiness-v1` recognizes only structured table recipes for `wooden_pickaxe` and `stone_pickaxe`, opens and validates the exact crafting-table window once, closes it, waits 100ms, and requires the normalized item-count inventory to remain exactly unchanged before the original craft is allowed. The preflight itself makes zero craft calls and world mutations; each Planner action still makes at most one original craft call and gains no backend retry. Readiness is shared by the later stone-pickaxe craft, while a wrong window, timeout, or inventory mutation becomes terminal without a second open or craft. Non-tool crafts and the existing 1000ms post-craft settlement remain unchanged. The shared Bridge, Mineflayer dependency, base protocol, Planner, action budget, and retained Phase 123 evidence are hash-protected. Audit `136fbf31...`, focused Phase 124 `3/3`, all stone-pickaxe `316/316`, TaskSystem/GoalVerifier/capability `140/140`, selected M1/M2/M3 `72/72`, full Python `1069/1069`, 1714 JSON files, compilation, PowerShell parsing, wrapper help, credential scan, and seven Node suites with 85 internal cases pass. No Minecraft process or live authorization runs; strict SP-003 remains 0/1 until a separately authorized baseline exercises the repair.
- Phase 125 pushes Phase 124 at `19d5780e`, consumes exactly one separate authorization at `c524b0c7`, and retains episode `sp003_baseline_20260720_051930_f39bab4c`. The full behavior chain closes again, while the former first-tool-craft no-op does not recur: action 16 makes one successful call and one attempt with zero retry and the exact wooden-pickaxe delta; action 24 uses the same table for the exact stone-pickaxe delta. The single raw failure is action 14, where a schema-valid Planner response incorrectly labels the table reference as stone surface clearance and requests a forbidden grass dig. The guard fails closed and action 15 correctly places the table, but strict surface-proof and zero-failure checks remain false. The integrated SP-001 verifier separately rejects `initial_movable` and raw-nearest-source transitions one and three because runtime access-rank sorting can choose a farther source. Manifest `0ef3e451...` binds all thirteen files. Evidence replay `3/3`, all stone-pickaxe Python `317/317`, full Python `1072/1072`, seven Node suites with 85 internal cases, 1726 JSON parses, compilation, PowerShell parsing, diff checks, and credential scanning pass; no retry, baseline, skill, capability, or M4 credit occurs.
- Phase 126 starts from pushed Phase 125 evidence commit `41b484a1` and adds policies `sp003-explicit-table-target-semantics-v1` and `sp003-sp001-machine-reachable-source-order-v1`. Planner targets now include literal true/false markers, and every clear table reference carries an explicit placement marker; no action rewrite or planner retry is added. SP-001 action snapshots retain the observed ground plus a bounded, fingerprinted list of only machine-proven pickup-access stones ordered by exact player-to-block distance. Invalid or tampered contracts fail closed, while snapshots without the new contract retain legacy semantics. Replaying the unchanged Phase 125 full observations through this path preserves all three selected sources and changes the derived SP-001 result from three issues to a clean pass. Focused Phase 126 passes 7/7, targeted SP-003 96/96, all stone-pickaxe Python 324/324, full Python 1079/1079, and seven Node suites 84/84; compilation, PowerShell, JSON, diff, retained-hash, and credential gates pass. Audit `5eecf4ad...` binds the transaction. No Minecraft process, authorization, baseline, skill, capability, or M4 credit occurs in the offline repair.
- Phase 127 pushes Phase 126 as `8d21c177`, then consumes exactly one separately pushed authorization `a9ead681`. Episode `sp003_baseline_20260720_064839_abb7c5cb` makes 12 real Planner calls with one transport attempt and zero retries each. Calls 0-10 are schema-valid. Actions 1-6 complete canopy egress, three distinct dark-oak logs, twelve planks, and four sticks. Call 6 incorrectly requests two extra planks during `craft_crafting_table`; the guard rejects it before the backend or any mutation, and call 7 recovers by crafting the table. Calls 8-10 prove the Phase 126 target semantics through one probe move and two explicit machine-proven surface clearances with no table-reference misread. Call 11 is rejected only for `reasoning_too_long`, ending the run `empty_plan` at 11 actions before table placement. Manifest `de2d72cd...` binds all thirteen retained evidence identities including itself. Evidence replay passes 3/3, the isolated replay of one transient scheduling-ablation failure passes 1/1, the final full repository rerun passes 1082/1082, 1868 JSON files parse, and credential scanning reports zero matches. No retry or evidence credit occurs; Phase 128 must address pre-dispatch stage semantic recovery and bounded reasoning before another authorization.
- Phase 128 adds SP-003-only policies `sp003-nonexecutive-reasoning-normalization-v1` and `sp003-pre-dispatch-semantic-replan-v1`. Only a nonblank reasoning string over 320 characters is truncated before envelope validation; raw provider response hash and byte count remain untouched, direct validation remains strict, and status, subtasks, and actions cannot be rewritten. The SP-003 runtime now retains the one planned action and, after ordinary runtime safety checks but before action counting or backend dispatch, suppresses only enumerated stage-semantic mismatches. It requests a fresh-observation next-cycle replan at most once per stage/issues/action/progress fingerprint and four times per episode; duplicates, unsafe target issues, and exhausted bounds fall through to the original fail-closed guard. The Phase 127 extra-plank shape integrates as two observations, one replan, zero bad-action backend calls, and one counted valid table craft. Core `Agent`, shared Bridge, preload, base protocol, Phase 122 overlay/runner, and Phase 127 evidence remain hash-identical. Focused Phase 128 passes 7/7, targeted SP-003 plus retained replay passes 102/102, all 26 stone-pickaxe test files pass 334/334, and the full repository passes 1089/1089. No Minecraft process or live authorization runs; behavioral and strict empty-hand-to-stone-pickaxe closure remain false until a separately pushed one-use baseline exercises the repair.
- Phase 132 starts from pushed Phase 131 evidence commit `00ee5271` and adds policy `sp003-grounded-approach-pre-dispatch-replan-v1`. It recovers only the singleton `sp003_stone_grounded_approach_required_before_dig` issue after the effective runtime guard has rejected an ungrounded direct dig against a navigation-only machine target. Recovery inherits the existing one-per-fingerprint and four-per-episode limits, requires a fresh observation, and adds no rewrite, same-call retry, backend invocation, action count, or world mutation. Duplicate, mixed, and unsafe cases remain fail-closed. The retained Phase 131 counterfactual becomes 22/22 successful actions with exactly three grounded stone digs and terminal `stone_pickaxe:1`. Audit `2c997729...`, focused 5/5, Phase 128-132 24/24, all stone-pickaxe 351/351, full Python 1106/1106, targeted regressions 161/161, Node 84/84, and 1766 JSON parses pass together with compilation, diff, and credential gates. No live process, authorization, baseline, skill, capability, or M4 credit occurs.
- Phase 133 pushes Phase 132 at `0393ba45` and consumes exactly one authorization at `77199001`. Episode `sp003_baseline_20260720_111853_3cd46332` executes one move and three distinct oak-log digs successfully, with zero action failures and terminal `oak_log:3`. The fixed provider takes `53.545s`, `47.641s`, `53.733s`, `60.422s`, and `56.907s`; the last response misses its remaining `55.907s` action window. The run ends `max_duration` at `300.985s` before any craft, so Phase 132 is not exercised. Manifest `8faabcec...` and all thirteen files replay 3/3; stone-pickaxe 354/354, full Python 1109/1109, and 1778 JSON parses pass with compilation, diff, and credential gates. No retry, baseline, skill, capability, or M4 credit occurs; provider throughput must be reestablished without Minecraft before another authorization.
- Phase 134 starts no Minecraft process and creates no authorization. It reuses the immutable Phase 133 observation immediately before Planner call 4: strict continuation stage `craft_matching_planks`, inventory `oak_log:3`, three distinct verified log removals, and an 11,287-byte two-message request. Exactly one fixed-provider attempt with zero retries returns a schema-valid `craft oak_planks count=12` action in `3.750s` for 2,643 prompt tokens, below the 7.5-second representative throughput gate. Artifact `f348f095...` records all twelve passing criteria without retaining credentials or raw response text. Focused probe tests pass 3/3, all stone-pickaxe tests pass 357/357, the full repository passes 1112/1112, and 1779 tracked or pending JSON files parse with compilation, diff, and credential gates clean. The probe grants no baseline, skill, capability, or M4 credit; after this evidence is committed and pushed, it permits only a separate parent-bound one-use baseline authorization.
- Phase 135 consumes exactly one parent-bound authorization from pushed commit `cf32589d` and retains episode `sp003_baseline_20260720_121934_e86b807f`. Provider throughput is healthy: 15/15 Planner calls are real and schema-valid, each has one attempt and zero retries, and cumulative provider time is `39.010s`. Action 3 removes `oak_log:119:141:-33` but its pickup postcondition fails, creating pending proof `a0b4b268...`. After action 4, session event 96 machine-observes `oak_log:3` with two verified sources and that one valid pending source, but the existing action-result-only reconciler does not advance progress. Planner call 4 therefore digs a fourth log; inventory becomes `oak_log:4`, and ten later exact 12-plank crafts fail closed on `sp003_exact_three_matching_logs_required`. The run ends `max_duration` at `308.828s`, with 4/15 successful actions, 11 preserved failures, no stone stage, and no retry. Manifest `9ed16b45...` binds all thirteen files. Evidence tests pass 3/3, all stone-pickaxe tests pass 360/360, the full repository passes 1115/1115, and 1791 JSON files parse with compilation, diff, and credential gates clean. No baseline, skill, capability, or M4 credit occurs; the next offline fix must reconcile a bounded pending removed-log proof from fresh machine inventory before stage and target selection.
- Phase 136 starts no Minecraft process and grants no authorization. Policy `sp003-observation-delayed-log-pickup-reconciliation-v1` admits only the first fresh observation whose inventory is exactly one log family, whose observed count is at most three, and whose surplus exactly matches oldest hash-valid pending removed-log proofs within the remaining three-source budget. Reconciliation runs before flags, stage, and target selection; it invokes no action or backend, consumes no planner budget, mutates no world state, preserves the raw failed action, and is idempotent. The immutable Phase 135 event-96 replay reaches three verified sources with zero pending sources in one observation, emits no fourth-log target, and admits exact `craft oak_planks count=12`; event-109 `oak_log:4`, stale, wrong-family, mixed-family, and forged controls all reject. The episode builder and independent verifier accept a synthetic preserved failure only with a matching observation proof and reject four evidence tamper cases. Focused counterfactual coverage passes 20/20, runtime/evidence 106/106, all stone-pickaxe 380/380, full Python 1135/1135, and TaskSystem/GoalVerifier/capability 134/134. All 1792 deliverable JSON files parse; compilation, diff, and credential gates pass. Audit `workspace/evals/stone_pickaxe_sp003_observation_pickup_reconciliation_repair.json` binds the repair and retained evidence. Baseline, skill, capability, and M4 counts remain unchanged; the next gate is commit/push before any separate one-use authorization.
- Phase 137 pushes Phase 136 at `9af7db44`, consumes exactly one separate authorization from `5aa70afa`, and retains episode `sp003_baseline_20260720_135522_c835c71d`. Preflight, reset, empty inventory, Paper, Bridge, and five log sources pass, but root Planner call 0 receives zero bytes and fails after one transport attempt with TLS EOF. No action executes and Phase 136 is not exercised. Manifest `553e8731...` plus derived failure `21ac9a4c...` classify the run as infrastructure-ineligible; evidence commit `fc6a78f3` is pushed with no retry or credit.
- Phase 138 runs one bounded root-provider probe from pushed tooling commit `bef42a1f` without Minecraft or authorization. The exact retained Phase 137 state produces one 13,525-byte request with 3,228 prompt tokens; the fixed provider returns a schema-valid five-node root graph and exact `move_to(121,142,-36)` first action in `5.625s`, with one request and zero retries. Artifact `8cd727c1...` passes all criteria and grants no baseline, skill, capability, or M4 credit. Its evidence must be committed and pushed before a separate parent-bound one-use baseline authorization may be created.
- Phase 139 pushes Phase 138 evidence at `21bd9764`, consumes exactly one separate authorization from `45aa6a8d`, and retains episode `sp003_baseline_20260720_150508_7a645e08`. Calls 0-2 are real and schema-valid; two distinct logs are removed, and one raw pickup failure is fully reconciled by existing action-result proof with no unreconciled action failure. The Phase 136 observation-only path remains unexercised. Continuation call 3 makes one zero-retry attempt, receives zero bytes, and fails with TLS EOF, ending the run after three actions. Manifest `1ead5fe3...` and derived record `80abdfa5...` grant no baseline, skill, capability, or M4 credit. The next gate is evidence push followed by one bounded no-Minecraft continuation-provider probe.
- SP-004 offline runtime commit `202b701` and smelt-health repair `c494565` are pushed
  on synchronized `main`. Live episode `sp004_live_20260724_032722` passed preflight
  with exact `stone_pickaxe:1`, two existing sticks, an observed crafting table, and a
  one-attempt/no-retry iron-ingot smelt contract. The first `grok-4.5` Planner call
  returned HTTP 502; session `b0774555-bb0` therefore ended `empty_plan` with zero
  gameplay actions and no world mutation by the agent. Six immutable evidence files
  retain the infrastructure failure, with no retry, capability credit, or M4 credit.
  The next gate is a bounded no-Minecraft provider probe after this evidence commit is
  pushed.
- After evidence commit `78d8123` was pushed, bounded SP-004 provider probe
  `sp004_provider_probe_20260724_034229` ran without Minecraft. Its only chat request
  used one attempt and zero retries and returned HTTP 502 after 6.625 seconds; a
  separate read-only models query was disconnected before a response. The provider is
  classified unavailable. No gameplay action, retry authorization, capability credit,
  or M4 credit was created.
- Resumed SP-004 audit r2 used canonical `/v1`, model `grok-4.5`, one attempt, and zero
  retries, and again returned HTTP 401 `AuthenticationError`. The new authentication
  blocker advances to 2/3 consecutive resumed goal turns without reusing the prior 502
  threshold. Hash-bound audit `sp004_resumed_authentication_audit_r2_20260724` records
  both r1 and r2. No Minecraft process, gameplay action, automatic retry, capability
  credit, or M4 credit occurred.
- Resumed SP-004 audit r3 again used canonical `/v1`, model `grok-4.5`, one attempt,
  and zero retries, and returned HTTP 401 `AuthenticationError`. The same resumed
  authentication blocker now reaches 3/3 consecutive goal turns and satisfies the
  strict blocked threshold. Hash-bound audit
  `sp004_resumed_authentication_blocked_audit_20260724` records all three probes. No
  Minecraft process, gameplay action, automatic retry, capability credit, or M4 credit
  occurred; progress requires an external valid-credential change.
- After a second goal resume, canonical probe
  `sp004_second_resume_audit_r1_20260724` again returned HTTP 401
  `AuthenticationError` with one attempt and zero retries. The fresh second-resume
  authentication audit starts at 1/3 and does not reuse either prior blocked threshold.
  No Minecraft process, gameplay action, automatic retry, capability credit, or M4
  credit occurred.
- Added the reusable SP-004 provider recovery gate
  `scripts/iron_pickaxe_sp004_provider_probe.py`. It is output-exclusive,
  evidence-scoped, bounded to one 15-second request, disables SDK retries, records no
  secret or raw response, and never starts Minecraft. Focused SP-004 coverage passes
  27/27. Recovery probe `sp004_provider_probe_20260724_035327` still returned HTTP 502
  after 6.594 seconds with one attempt and zero retries, so no new live episode is
  authorized.
- Added controlled launcher `scripts/iron-pickaxe-sp004-runtime.ps1`. It checks
  synchronized protected runtime state, fresh evidence/world paths, EULA/operator and
  port prerequisites, then runs the provider gate before any process start. A passing
  gate alone may start Paper and the isolated bridge, construct the exact audited
  fixture, and invoke one bounded episode; cleanup always stops owned processes and
  restores server properties. PowerShell parsing and focused SP-004 coverage pass
  32/32. No provider request or Minecraft process was started while validating the
  launcher.
- Fixed the provider recovery gate to normalize every configured endpoint to `/v1`;
  focused SP-004 coverage passes 33/33 and fix commit `d3e5115` is pushed. The first
  turn-three diagnostic lacked canonical `/v1` and is retained but excluded from the
  blocked threshold. The replacement probe
  `sp004_recovery_audit_turn3_equivalent_20260724` proves canonical `/v1`,
  `grok-4.5`, JSON response mode, thinking disabled, bounded timeout, one attempt, and
  zero retries, but still received HTTP 502 with zero response bytes. Together with
  the two prior canonical probes, this satisfies three consecutive goal turns of the
  same external provider blocker. No Minecraft process, gameplay action, automatic
  retry, capability credit, or M4 credit occurred.
- After the blocked goal resumed, canonical probe
  `sp004_resumed_audit_r1_20260724` reached the configured `/v1` endpoint but returned
  HTTP 401 `AuthenticationError` with one attempt and zero retries. This differs from
  the prior three-turn HTTP 502 blocker and starts a fresh resumed audit at 1/3.
  The provider gate now records HTTP status and distinguishes authentication,
  availability, and transport/protocol failures; focused SP-004 coverage passes 34/34.
  No credential value, Minecraft process, gameplay action, retry, capability credit,
  or M4 credit was created.
- The user demonstrated a successful direct `grok-4.5` request, and a canonical
  zero-retry recovery probe passed. Controlled episode `sp004_live_20260724_090802`
  passed exact preflight; root Planner call 0 was schema-valid and action 1 mined one
  distinct stone successfully. Continuation call 1 made one request with zero retries
  and returned `finish_reason=stop` plus 203 reasoning bytes but zero ordinary content
  bytes, so strict parsing ended `empty_plan`. The immutable episode grants no
  capability or M4 credit.
- Added default-off provider policy `sp004-reasoning-content-json-fallback-v1`, enabled
  only by SP-004 configuration. Empty ordinary content may use reasoning content only
  when the entire value parses as one JSON object; unchanged plan schema and action
  guards still apply, and no request or retry is added. Non-JSON, array, and fenced
  controls fail closed. Targeted Python regressions pass 327/327 and Node smelt
  regressions pass 6/6.
- Episode `sp004_live_20260724_093304` made four valid zero-retry Planner calls,
  completing one move and three distinct stone digs with no action failure. Call 4
  returned stop with zero content bytes and 203 non-object reasoning bytes; the run
  therefore ended `empty_plan` with no capability or M4 credit. Added default-off,
  SP-004-only policy `sp004-forced-json-tool-output-v1`: one forced `submit_json`
  function carries the model's plan arguments, while the existing schema and action
  guard remain authoritative and no request or retry is added.
- No-Minecraft probe `sp004_forced_tool_probe_20260724_0945` successfully exercised
  the forced tool argument channel with one request and zero retries, but failed its
  exact-object check because the permissive tool schema admitted an extra field.
  Minecraft did not start. The tool contract now requires exactly the seven SP-004
  plan fields and forbids additional properties before the unchanged validator runs.
- No-Minecraft probe `sp004_forced_schema_probe_20260724_0950` passed all criteria:
  one request, zero retries, exact seven-field object, and the forced tool argument
  channel. It started no Minecraft process and grants no capability or M4 credit.
- Live episode `sp004_live_20260724_095220` proved the structured tool channel no
  longer failed empty, but ended at the 1800-second deadline after generic dusk/night
  shelter interrupts and a terminal provider timeout. It completed 33 recorded
  actions but only four verified stone removals, with no coal, iron, furnace, smelt,
  or iron pickaxe. No retry, capability credit, or M4 credit was granted.
- Added SP-004-only policy `sp004-peaceful-shelter-interrupt-isolation-v1`.
  It suppresses only dusk/night shelter scheduling in the peaceful fixture while
  preserving health, hunger, hostile, task, and absolute-deadline safety checks.
- Episode `sp004_live_20260724_112322` exercised that isolation but revealed the
  fixture had no support plane: its first x/z move fell from Y=137 to Y=62.77, one
  natural stone was removed without pickup, and 57 navigation replans then failed
  before a final zero-retry HTTP 502. Added `sp004-supported-fixture-platform-v1`:
  a cobblestone floor, three-block clearance, and player recentering while preserving
  the exact 8 stone, 10 coal ore, 3 iron ore, and one-table contract.
- Episode `sp004_live_20260724_115339` kept the player on the new Y=145 platform,
  used only two navigation actions, and verified four distinct stone sources. Pickup
  then changed the selected held item while `stone_pickaxe:1` remained in inventory;
  55 later digs were guard-rejected. Added `sp004-resource-tool-requip-v1`, permitting
  only an explicit LLM-planned `equip stone_pickaxe` during stone/coal/iron stages
  when the exact tool is present and not already held.
- Episode `sp004_live_20260724_124204` completed exact 8 stone, 10 coal, and 3 iron
  source removal and crafted one furnace. It then repeated ten failed placements
  because the table coordinate was incorrectly used as the supporting reference,
  targeting the cell above the table. Added
  `sp004-open-support-furnace-reference-v1`, which requires a machine-observed solid
  support with an open cell above for furnace placement.
- Episode `sp004_live_20260724_130921` passed preflight and made five successful
  actions with three verified stone sources before a single zero-retry Planner call
  returned HTTP 502. The new furnace-placement repair was not reached. The retained
  episode is infrastructure-ineligible and grants no capability or M4 credit.
- No-Minecraft probe `sp004_post502_recovery_probe_20260724_1320` passed all provider
  recovery criteria in one zero-retry request using the forced structured channel.
- Episode `sp004_live_20260724_132407` reached five verified stone sources, but one
  additional drop fell to Y=136 and timed out pickup; the initial observation exposed
  no cobblestone support. Added `sp004-machine-verified-sky-platform-v1`: fixed
  fixture Y=200 plus mandatory player-height and cobblestone-below machine preflight.
- Episode `sp004_live_20260724_134510` verified that repair, completing 22 accepted
  actions with exact 8 stone, exact 10 coal, and 1 of 3 iron before planner call 22
  returned a single zero-retry HTTP 502. No action failed. The episode is retained as
  infrastructure-ineligible and grants no lifecycle, capability, or M4 credit.
- No-Minecraft probe `sp004_post502_recovery_probe_20260724_140031` passed all
  provider recovery criteria in one zero-retry forced-structured request.
- Preflight `sp004_live_20260724_140408` started no gameplay: its provider gate
  passed, but the machine support check caught an unconfirmed platform fill before
  the unsupported high-altitude player could enter the episode. Added
  `sp004-segmented-machine-verified-platform-v1`: bootstrap support before teleport,
  temporary fixture flight, segmented fills, and six exact floor sentinels.
- Preflight `sp004_live_20260724_141411` also started no gameplay. Server evidence
  showed the central fill succeeded while adjacent resource chunks were not loaded.
  Added `sp004-forceloaded-machine-verified-platform-v1`, which force-loads the
  bounded fixture rectangle before filling and preserves six exact sentinel checks.
- Provider gate `sp004_live_20260724_142451` stopped before Minecraft startup with
  HTTP 403 `unauthenticated:bad-credentials`. It made one request, zero retries, and
  zero gameplay actions. The force-loaded platform repair is not yet exercised.
- Independent no-Minecraft probe `sp004_auth_recovery_probe_20260724_143244`
  observed a changed upstream failure: HTTP 503 `auth_unavailable: no auth
  available` for xAI `grok-4.5`, again with one request and zero retries. This is an
  external provider hold and grants no lifecycle, capability, or M4 credit.
- Independent no-Minecraft probe `sp004_auth_recovery_probe_20260724_143844`
  passed all provider recovery criteria in one zero-retry forced-structured request.
  The external provider hold is cleared for a new live episode.
- Preflight `sp004_live_20260724_144535` started no gameplay but proved every
  force-loaded platform segment was created. Its support lookup could floor a
  fractional post-teleport Y onto the layer below the real support. Added
  `sp004-support-block-ceil-minus-one-v1` and preserved six resource sentinels.
